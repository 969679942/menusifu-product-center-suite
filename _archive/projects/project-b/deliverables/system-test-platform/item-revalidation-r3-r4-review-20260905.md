# 商品中心 r3/r4 执行复核

## 结果与边界

- r3：196 条执行结束，193 条通过，3 条运行失败；本轮未重新启动业务执行。
- 复核期间发现工作区已有 r4：仅重验上述 3 条，3 条运行通过。保留原始 r3 与 r4，不把 r3 失败覆盖到新结果。
- 当前生命周期输出：628 = 427 + 201；408 = 408 + 0；427 != 408，仍有 19 条身份对账缺口。
- r4 的 3 条 assertionReceipts 均为空，虽然导入器将其标记 complete，严格断言证据闭环仍需核对；数字归零不代表完全验收。
- 本次修改范围为 project-adapter / generated-evidence；已有业务结果不改、不重跑，不启动跨 applicationId 试点。

## 失败诊断

| 用例 | r3 原因 | r4 运行状态 | 当前复核状态 |
| --- | --- | --- | --- |
| TC-ITEM-STD-019 | 导航超时，transient-platform | passed | 断言收据待核对 |
| TC-ITEM-PKG-021 | 输入被截断，通用输入稳定性检查提前抛错 | passed | 断言收据待核对 |
| TC-ITEM-ADD-012 | 输入被截断，未进入保存后格式化断言 | passed | 断言收据待核对 |

以上不能认定为产品缺陷。r3 没有完整标准收据；清理附件的详细范围见配套 JSON，不把空对象集合描述为完整业务清理验证。

## 必须处理的最小范围

1. 19 条图片/标签用例先核对身份谱系。旧执行器把整份绑定文件指纹当作逐用例指纹，重跑前必须先解决写入与消费口径，禁止直接替换历史指纹。
2. 3 条 r4 用例核对实际断言步骤与附件。公共导入器应验证结构化断言，而不是只信任 claims.verified。公共修复先做独立影响评估与隔离负向测试，不直接扩大业务重跑。
3. 已修复报告时效评估：使用当前时间评估，固定时间重放仍可用；不延长 observedAt/freshUntil，不因此失效通过用例。17 项针对性合同测试通过。

目的：让对账缺口和真实执行问题分开收敛。预期结果：每条身份和断言证据可追溯。后续影响：22 条进入静态证据复核，不等于 22 条重跑授权；本轮不宣布全局闭环。

完整来源路径、SHA-256、19 条名单及逐案诊断见同名 JSON。生命周期与审计 HTML 是当前派生视图；本复核文件是带时间和输入指纹的历史快照。
