# 7. 需求品牌调味-自动化测试用例

对应需求：[7.需求品牌调味.md](7.需求品牌调味.md)

> 自动化块来源：`3.商品中心-商品管理-调味管理-自动化测试用例.md`；按**用例标题**匹配，不新增步骤。

---

# 商品中心-商品管理-调味管理 自动化测试用例

> 由 PRD 需求测试用例按标题从 `3.商品中心-商品管理-调味管理-自动化测试用例.md` 提取，不新增自动化步骤；规范见 [product-center-automation-test-case](.cursor/skills/product-center-automation-test-case/SKILL.md)。

## 调味管理自动化套件约定

> 写入 `3.商品中心-商品管理-调味管理-自动化测试用例.md` 顶部，**每条用例不再重复**。

## 登录与权限

- 使用执行计划绑定的测试账号登录商品中心（品牌视角）
- 门店/POS 用例另需门店上下文 S1 与 POS **30.16.6.7** 环境
- 账号须具备商品管理 / 调味管理读写权限

## 数据命名

| 类型 | 格式 | 示例 |
|------|------|------|
| 品牌调味组 | `TC_FLV_GRP_{正式编号}` | `TC_FLV_GRP_015` |
| 品牌调味项 | `TC_FLV_ITEM_{正式编号}` | `TC_FLV_ITEM_015` |
| 调味模板 | `TC_FLV_TPL_{正式编号}` | `TC_FLV_TPL_008` |
| 下发记录查询 | `TC_FLV_REC_{正式编号}` | `TC_FLV_REC_002` |
| 门店引用 | `TC_FLV_STORE_S1` / `TC_FLV_REF_{编号}` | `TC_FLV_REF_011` |
| PRD 需求前缀 | `D7_` / `D8_` / `D10_` + 场景 | `D8_模板下发-TC-需求8-020` |

- 执行前确认造数对象已存在（Readiness 未通过则 block/skip）

## 数据依赖用例（UI + 平台造数）

> 本文档 **仅含 UI/E2E 可执行用例**；接口造数/清理 **不写入本文档**。

- 前置含「已下发」「已存在模板/调味组/下发记录」→ 须写 **`数据标识`** + **Readiness**
- 步骤按固定名称/查询关键字定位，禁止「找一条满足条件的调味」

## 导航入口

| 子模块 | 路径 |
|--------|------|
| 品牌调味 | 商品管理 → 调味管理 → 调味 |
| 调味模板 | 商品管理 → 调味管理 → 调味模板 |
| 下发记录 | 商品管理 → 调味管理 → 下发记录 |
| 门店调味 | 门店商品管理 → 门店调味 |
| POS 全局调味 | POS 门店 S1 全局调味/点单入口（E2E） |

- 起始页与步骤第 1 步一致；保存/确认/查询统一加引号

## 跨模块 / POS（TC-FLV-XMOD-* / TC-FLV-POS-*）

- `TC-FLV-POS-*` 与部分 `TC-FLV-XMOD-*` 为 **E2E**
- 品牌下发、模版下发、菜单下发、同步 POS 由执行计划承担

## 自动化生成注意

- 正式前置里的 **「测试数据满足本场景（调味组/调味项/模板/下发记录等）」** 为占位句，**不触发** `数据标识` / Readiness
- **缺省页新建**（如 SEA-009）：前置应为「当前品牌下无调味数据」，步骤须字段级展开，禁止「点击新增入口，填写表单并确认保存」
- 命名：`TC_FLV_GRP_{正式编号}`、`TC_FLV_ITEM_{正式编号}`、`TC_FLV_TPL_{正式编号}`；PRD 小需求另用 `D7_/D8_/D10_` 前缀

---


## 调味模板

### 用例编号：TC-需求7-001

用例标题：无调味时缺省页展示行业调味库入口说明

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现调味列表或行业调味库入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 查看无调味时的缺省页文案与「选择调味/行业调味库」入口。

预期结果：
1. 行业调味库：我们按照行业类型，也提供了丰富的通用调味供您选择。

---

### 用例编号：TC-需求7-002

用例标题：选择调味页面按调味组与调味项展示正确

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现调味列表或行业调味库入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 点击「选择调味」或行业调味库入口。
2. 查看调味组+调味项列表字段。

预期结果：
1. 数据展示格式：调味组+调味。
2. 调味组信息：调味组名称、调味组第二语言、调味数量。
3. 调味信息：调味名称、调味第二语言名称。

---

### 用例编号：TC-需求7-003

用例标题：有调味时列表展示调味组名称、第二语言与调味数量

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现调味列表或行业调味库入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 查看品牌调味列表中调味组/调味项各列字段。

预期结果：
1. 调味组信息展示：调味组名称、调味组第二语言、调味数量。

---

### 用例编号：TC-需求7-004

用例标题：有调味时列表展示调味项名称、第二语言、应用门店、状态与价格

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现调味列表或行业调味库入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 查看品牌调味列表中调味组/调味项各列字段。

预期结果：
1. 调味信息展示：调味名称、第二语言、应用门店、状态（默认启用）、价格（创建时不填写价格默认展示0）。

---

### 用例编号：TC-需求7-005

用例标题：新增调味页面字段展示正确

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现调味列表或行业调味库入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 点击「新增调味」。
2. 「调味组名称」输入 `TC_FLV_GRP_005`；「调味名称」输入 `TC_FLV_ITEM_005`。
3. 点击「确定」。

预期结果：
1. 调味组信息展示：调味组名称、调味组第二语言、pos名称。
2. 调味信息字段展示：调味名称、第二语言、调味价格、pos名称、送厨名称、pos颜色。

---

### 用例编号：TC-需求7-006

用例标题：批量操作未勾选置灰、勾选后高亮并展示已选数量

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现调味列表或行业调味库入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 勾选调味项后执行批量操作。

预期结果：
1. 未勾选调味时置灰展示。
2. 勾选调味后高亮展示，变为“可用”状态，展示选择的调味数量，对应字段“X”。

---

### 用例编号：TC-需求7-007

用例标题：按调味名称模糊或精确查询返回正确结果

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 输入查询条件并执行查询。

预期结果：
1. 按调味名称模糊/精确查询正常。

---

### 用例编号：TC-需求7-008

用例标题：调味列表查询后重置条件清空并恢复全量数据

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现调味列表或行业调味库入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 输入查询条件并执行查询。

预期结果：
1. 重置。

---

### 用例编号：TC-需求7-009

用例标题：缺省页点击新增调味可跳转新增页并保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现调味列表或行业调味库入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 当前品牌下无调味数据（teardown 后或空品牌）。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 确认当前为无调味缺省页（可见行业调味库说明或「新增调味」入口）。
2. 点击「新增调味」。
3. 确认页面跳转至【新增调味】表单页。
4. 「调味组名称」输入 `TC_FLV_GRP_009`。
5. 「调味名称」输入 `TC_FLV_ITEM_009`。
6. 「默认价格」输入 `10.00`。
7. 点击「确定」。
8. 返回列表，按 `TC_FLV_GRP_009` 查询或浏览列表，确认新建调味组及调味项存在。

预期结果：
1. 跳转至【新增调味】页面。
2. 出现保存/操作成功提示。
3. 列表可查到 `TC_FLV_GRP_009` 调味组及 `TC_FLV_ITEM_009` 调味项。

---

### 用例编号：TC-需求7-010

用例标题：缺省页使用行业通用调味可选择并创建成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现调味列表或行业调味库入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 当前品牌下无调味数据（teardown 后或空品牌）。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 确认当前为无调味缺省页。
2. 点击「使用行业通用调味」或「选择调味」入口。
3. 确认跳转至【选择调味】页面，列表展示行业预制调味组+调味项。
4. 勾选至少一条行业调味并点击「确认」。

预期结果：
1. 点击使用行业通用调味可跳转【选择调味】页面，页面展示行业商品库中调味，并且可以选择成功。

---

### 用例编号：TC-需求7-011

用例标题：新增调味组必填项为空时保存失败并高亮缺失字段

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现「新增调味」或目标操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 点击「新增调味」。
2. 确认进入【新增调味】表单页。
3. 「调味组名称」留空；「调味名称」留空。
4. 点击「确定」。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 新增调味组，其中有必填参数为空，点击确认提示必填项未填写，缺失的必填项高亮提示。

---

### 用例编号：TC-需求7-012

用例标题：新增调味项有字段值但名称为空时保存失败并提示

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现「新增调味」或目标操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 点击「新增调味」。
2. 「调味组名称」输入 `TC_FLV_GRP_012`。
3. 「默认价格」输入 `10.00`；「调味名称」留空。
4. 点击「确定」。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 新增调味信息，调味信息任意字段有数据，调味名称为空，点击确认提示请填写调味名称，缺失的必填项高亮提示。

---

### 用例编号：TC-需求7-013

用例标题：已有调味组下新增调味项缺必填项时仅本次失败且原组不变

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_GRP_013`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现「新增调味」或目标操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_GRP_013` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 打开已有调味组 `TC_FLV_GRP_013`，点击「添加调味」。
2. 「调味名称」留空；「默认价格」输入 `10.00`。
3. 点击「确定」。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 已有的调味组，新增调味信息，缺少必填项，新的调味信息创建失败，调味组还是原来的信息。

---

### 用例编号：TC-需求7-014

用例标题：调味组与调味名称支持中英文数字符号且不支持表情

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 点击「新增调味」。
2. 「调味组名称」输入 `TC_FLV_GRP_014_中文A1`；「调味名称」输入 `TC_FLV_ITEM_014_En1#`。
3. 点击「确定」，确认合法字符保存成功。
4. 再次「新增调味」，在名称字段输入表情符号，确认无法录入或保存失败。

预期结果：
1. 调味组、调味名称，支持中文、英文、数字、特殊符号，不支持表情。

---

### 用例编号：TC-需求7-015

用例标题：调味价格为0~999999.99且最多2位小数时保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 点击「新增调味」。
2. 「调味组名称」输入 `TC_FLV_GRP_015`；「调味名称」输入 `TC_FLV_ITEM_015`。
3. 「默认价格」依次验证 `0`、`10.50`、`999999.99` 均可保存（可分次新建）。

预期结果：
1. 输入必须为≥0数字，支持2位小数，输入限制 0 ≤ 输入数字 ≤ 999999.99元；。

---

### 用例编号：TC-需求7-016

用例标题：调味价格为负数时保存失败并提示输入数字异常

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 点击「新增调味」。
2. 「调味组名称」输入 `TC_FLV_GRP_016`；「调味名称」输入 `TC_FLV_ITEM_016`。
3. 「默认价格」输入 `-1`。
4. 点击「确定」。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 输入负数时，提示输入数字异常；。

---

### 用例编号：TC-需求7-017

用例标题：调味价格超过999999.99时保存失败并提示超出上限

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 点击「新增调味」。
2. 「调味组名称」输入 `TC_FLV_GRP_017`；「调味名称」输入 `TC_FLV_ITEM_017`。
3. 「默认价格」输入 `1000000`。
4. 点击「确定」。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 输入超出999999.99时，提示输入超出上限。

---

### 用例编号：TC-需求7-018

用例标题：新增调味组仅填必填项保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现「新增调味」或目标操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 确认当前为无调味缺省页（可见行业调味库说明或「新增调味」入口）。
2. 点击「新增调味」。
3. 确认页面跳转至【新增调味】表单页。
4. 「调味组名称」输入 `TC_FLV_GRP_018`。
5. 「调味名称」输入 `TC_FLV_ITEM_018`。
6. 「默认价格」输入 `10.00`。
7. 点击「确定」。
8. 返回列表，按 `TC_FLV_GRP_018` 查询或浏览列表，确认新建调味组及调味项存在。

预期结果：
1. 新增调味组，只填必填参数，能新增成功。

---

### 用例编号：TC-需求7-019

用例标题：新增调味组填写全部字段保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现「新增调味」或目标操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 确认当前为无调味缺省页（可见行业调味库说明或「新增调味」入口）。
2. 点击「新增调味」。
3. 确认页面跳转至【新增调味】表单页。
4. 「调味组名称」输入 `TC_FLV_GRP_019`。
5. 「调味名称」输入 `TC_FLV_ITEM_019`。
6. 「默认价格」输入 `10.00`。
7. 点击「确定」。
8. 返回列表，按 `TC_FLV_GRP_019` 查询或浏览列表，确认新建调味组及调味项存在。

预期结果：
1. 新增调味组，所有字段都添加了，能新增成功。

---

### 用例编号：TC-需求7-020

用例标题：新增调味组同时新增调味项保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现「新增调味」或目标操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 确认当前为无调味缺省页（可见行业调味库说明或「新增调味」入口）。
2. 点击「新增调味」。
3. 确认页面跳转至【新增调味】表单页。
4. 「调味组名称」输入 `TC_FLV_GRP_020`。
5. 「调味名称」输入 `TC_FLV_ITEM_020`。
6. 「默认价格」输入 `10.00`。
7. 点击「确定」。
8. 返回列表，按 `TC_FLV_GRP_020` 查询或浏览列表，确认新建调味组及调味项存在。

预期结果：
1. 新增调味组+调味信息，能新增成功。

---

### 用例编号：TC-需求7-021

用例标题：已有调味组下新增调味项保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_GRP_021`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现「新增调味」或目标操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_GRP_021` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 确认当前为无调味缺省页（可见行业调味库说明或「新增调味」入口）。
2. 点击「新增调味」。
3. 确认页面跳转至【新增调味】表单页。
4. 「调味组名称」输入 `TC_FLV_GRP_021`。
5. 「调味名称」输入 `TC_FLV_ITEM_021`。
6. 「默认价格」输入 `10.00`。
7. 点击「确定」。
8. 返回列表，按 `TC_FLV_GRP_021` 查询或浏览列表，确认新建调味组及调味项存在。

预期结果：
1. 已有的调味组，新增调味信息，可新增成功。

---

### 用例编号：TC-需求7-022

用例标题：调味组下调味项达50个上限后不可再添加

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 打开已含 50 个调味项的调味组 `TC_FLV_GRP_022_MAX`（执行计划造数）。
2. 点击「+添加调味」。
3. 确认出现「调味数据已达到最大上限（50）」提示，未新增第 51 项。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 新增。
3. 移动。

---

### 用例编号：TC-需求7-023

用例标题：二次选择行业调味同组同名时提示已存在

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_GRP_023`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现调味列表或行业调味库入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_GRP_023` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 打开「使用行业通用调味/选择调味」。
2. 选择一条行业调味并确认加入列表。
3. 再次打开选择页，勾选同调味组下同名调味项。
4. 确认提示「已存在相同调味」或等价文案。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 同调味组下同名，提醒已存在相同调味。

---

### 用例编号：TC-需求7-024

用例标题：调味组名称或第二语言全局重复时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 确认品牌下已存在对象 `TC_FLV_GRP_024_DUP`（执行计划造数）。
2. 点击「新增调味」。
3. 「调味组名称」输入与 `TC_FLV_GRP_024_DUP` 冲突的值；「调味组名称」/「调味名称」填写 `TC_FLV_GRP_024` / `TC_FLV_ITEM_024` 等必填项。
4. 点击「确定」。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 调味组名称、调味组第二语言名称全局唯一，不可重复。

---

### 用例编号：TC-需求7-025

用例标题：调味组名称与第二语言互斥重复时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 确认已存在调味组名称 `TC_FLV_GRP_025_A`、第二语言 `TC_FLV_GRP_025_B`（执行计划造数）。
2. 点击「新增调味」。
3. 「调味组名称」输入 `TC_FLV_GRP_025_B`（与已有第二语言冲突）；「调味名称」输入 `TC_FLV_ITEM_025`。
4. 点击「确定」。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 调味组名称、调味组第二语言名称互相不能重复。

---

### 用例编号：TC-需求7-026

用例标题：调味组或调味项名称重复时保存失败并提示内容重复

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 确认已存在 `TC_FLV_GRP_026_DUP`（执行计划造数）。
2. 点击「新增调味」。
3. 「调味组名称」输入 `TC_FLV_GRP_026_DUP`；「调味名称」输入 `TC_FLV_ITEM_026`。
4. 点击「确定」。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 有重复数据，点击保存时，提示内容重复。

---

### 用例编号：TC-需求7-027

用例标题：同一调味组下调味名称或第二语言重复时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 打开调味组 `TC_FLV_GRP_027`（组内已有 `TC_FLV_ITEM_027`，执行计划造数）。
2. 点击「添加调味」。
3. 「调味名称」输入与 `TC_FLV_ITEM_027` 相同的名称。
4. 点击「确定」。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 调味组下调味名称、第二语言不可重复。

---

### 用例编号：TC-需求7-028

用例标题：删除调味组下调味项成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_GRP_028`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_GRP_028` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 在列表定位调味组 `TC_FLV_GRP_028` 并展开（Readiness：组内须已有 `TC_FLV_ITEM_028`）。
2. 勾选或选中调味项 `TC_FLV_ITEM_028`。
3. 点击「删除」。
4. 在二次确认弹窗点击「确认」。
5. 确认列表中 `TC_FLV_GRP_028` 下不再展示 `TC_FLV_ITEM_028`。

预期结果：
1. 调味组下调味信息删除正常。

---

### 用例编号：TC-需求7-029

用例标题：调味组下有调味项时可删除调味组成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_GRP_029`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_GRP_029` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 在列表定位调味组 `TC_FLV_GRP_029` 并展开（Readiness：组内须已有 `TC_FLV_ITEM_029`）。
2. 勾选或选中调味项 `TC_FLV_ITEM_029`。
3. 点击「删除」。
4. 在二次确认弹窗点击「确认」。
5. 确认列表中 `TC_FLV_GRP_029` 下不再展示 `TC_FLV_ITEM_029`。

预期结果：
1. 调味组下有调味信息，可删除成功。

---

### 用例编号：TC-需求7-030

用例标题：调味组下无调味项时可删除调味组成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_GRP_030`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_GRP_030` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 在列表定位调味组 `TC_FLV_GRP_030` 并展开（Readiness：组内须已有 `TC_FLV_ITEM_030`）。
2. 勾选或选中调味项 `TC_FLV_ITEM_030`。
3. 点击「删除」。
4. 在二次确认弹窗点击「确认」。
5. 确认列表中 `TC_FLV_GRP_030` 下不再展示 `TC_FLV_ITEM_030`。

预期结果：
1. 调味组下没有调味信息，可删除成功。

---

### 用例编号：TC-需求7-031

用例标题：删除调味组或调味项时弹出二次确认

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_GRP_031`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_GRP_031` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 定位调味组 `TC_FLV_GRP_031` 或组内调味项 `TC_FLV_ITEM_031`。
2. 点击「删除」。
3. 确认弹出二次确认弹窗后点击「确认」。

预期结果：
1. 调味组/调味信息删除时有二次确认按钮。

---

### 用例编号：TC-需求7-032

用例标题：编辑调味组信息保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_GRP_032`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现「新增调味」或目标操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_GRP_032` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 打开调味组 `TC_FLV_GRP_032` 编辑入口。
2. 修改「调味组第二语言名称」为 `TC_FLV_GRP_032_SL`（或其他可观测字段）。
3. 点击「确定」。
4. 确认列表中 `TC_FLV_GRP_032` 展示已更新。

预期结果：
1. 编辑调味组信息正常。

---

### 用例编号：TC-需求7-033

用例标题：编辑调味项信息保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_GRP_033`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现「新增调味」或目标操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_GRP_033` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 在列表定位调味组 `TC_FLV_GRP_033`，打开调味项 `TC_FLV_ITEM_033` 编辑入口。
2. 修改「默认价格」为 `12.00`（或其他可观测字段）。
3. 点击「确定」。
4. 确认 `TC_FLV_ITEM_033` 详情/列表展示已更新。

预期结果：
1. 编辑调味信息正常。

---

### 用例编号：TC-需求7-034

用例标题：新增调味组或调味项点击取消不落库

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现「新增调味」或目标操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 点击「新增调味」。
2. 「调味组名称」输入 `TC_FLV_GRP_034`；「调味名称」输入 `TC_FLV_ITEM_034`。
3. 点击「确定」。

预期结果：
1. 新增调味组/调味信息，填入参数，点取消按钮，不会新增成功。

---

### 用例编号：TC-需求7-035

用例标题：编辑调味组或调味项点击取消不保存变更

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_GRP_035`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现「新增调味」或目标操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_GRP_035` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 在列表定位调味组 `TC_FLV_GRP_035`，打开调味项 `TC_FLV_ITEM_035` 编辑入口。
2. 修改「默认价格」为 `12.00`（或其他可观测字段）。
3. 点击「确定」。
4. 确认 `TC_FLV_ITEM_035` 详情/列表展示已更新。

预期结果：
1. 编辑调味组/调味信息，填入参数，点取消按钮，不会编辑成功。

---

### 用例编号：TC-需求7-036

用例标题：删除调味组或调味项点击取消不执行删除

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_GRP_036`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_GRP_036` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 定位目标 `TC_FLV_GRP_036` 或 `TC_FLV_ITEM_036`。
2. 点击「删除」。
3. 在二次确认弹窗点击「确认」。

预期结果：
1. 删除调味组/调味信息，填入参数，点取消按钮，不会删除成功。

---

### 用例编号：TC-需求7-037

用例标题：批量变更调味组成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现「新增调味」或目标操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 勾选调味项后执行批量操作。

预期结果：
1. 验证批量变更调味组正常。

---

### 用例编号：TC-需求7-038

用例标题：手动拖动调味项排序保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 拖动调味项或调味组调整顺序并保存。

预期结果：
1. 验证可以手动拖动调味进行排序。

---

### 用例编号：TC-需求7-039

用例标题：调味组排序保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 拖动调味项或调味组调整顺序并保存。

预期结果：
1. 支持调味组排序。

---

### 用例编号：TC-需求7-040

用例标题：单门店品牌一键下发完全替换门店调味成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现「新增调味」或目标操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 选择门店/模板并执行下发。

预期结果：
1. 可正常下发至门店-全局调味，下发后门店-全局调味信息与商品-全局调味中一致。

---

### 用例编号：TC-需求7-041

用例标题：多门店品牌调味页不展示直接下发按钮

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_GRP_043`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面出现调味列表或行业调味库入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_GRP_043` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 选择门店/模板并执行下发。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 品牌有多个门店时-不展示下发按钮。

---

### 用例编号：TC-需求7-042

用例标题：不同调味组下同名调味项保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_GRP_044`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_GRP_044` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入品牌调味页。

测试步骤：
1. 打开 商品管理 → 调味管理 → 调味，新增调味组 G2。
2. 在 G2 下新增调味项，名称填写「辣酱」（与 G1 下调味项同名），其余必填项合法。
3. 保存并查看列表。

预期结果：
1. 保存成功，无重复名称报错。
2. G1、G2 下均可看到名为「辣酱」的调味项。

---

### 用例编号：TC-需求7-043

用例标题：调味组名或调味项名称达100字符边界保存成功

优先级：P2

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。
3. 准备恰好 100 个字符的组名/项名（中英文均可）。

测试步骤：
1. 新增调味组，组名称填写 100 字符，保存。
2. 在该组下新增调味项，调味名称填写 100 字符，价格等必填合法，保存。

预期结果：
1. 组名 100 字符保存成功，列表展示完整或按现网截断规则展示。
2. 调味项名称 100 字符保存成功。

---

### 用例编号：TC-需求7-044

用例标题：调味价格输入超过两位小数保存时四舍五入为两位

优先级：P2

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入品牌调味页。

测试步骤：
1. 在价格字段输入 `1.235`，保存。
2. 在价格字段输入 `1.234`，保存（或新建另一条调味项）。
3. 查看列表/详情中的价格展示。

预期结果：
1. `1.235` 保存后为 `1.24`（四舍五入）。
2. `1.234` 保存后为 `1.23`（四舍五入）。
3. 保存成功，无格式错误提示。

---

