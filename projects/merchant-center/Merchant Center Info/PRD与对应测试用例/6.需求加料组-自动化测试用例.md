# 6. 需求加料组-自动化测试用例

对应需求：[6.需求加料组.md](6.需求加料组.md)

| 章节 | 条数 |
|------|------|
| 加料组管理 | 31 |
| S02 加料商品 | 3 |
| S07 加料商品规则 | 3 |
| S08 商品内加料组 | 2 |
| S07 补充 | 3 |
| S08 补充 | 1 |

> 自动化块来源：`1.商品中心-商品管理-商品-自动化测试用例.md`、`2.商品中心-商品管理-组-自动化测试用例.md`；按**用例标题**匹配，不新增步骤。

---

# 商品中心-商品管理-组 自动化测试用例

> 由 PRD 需求测试用例按标题从 `2.商品中心-商品管理-组-自动化测试用例.md` 提取，不新增自动化步骤；规范见 [product-center-automation-test-case](.cursor/skills/product-center-automation-test-case/SKILL.md)。

## 组管理自动化套件约定

> 写入 `*-组-自动化测试用例.md` 顶部，**每条用例不再重复**。

## 登录与权限

- 使用执行计划绑定的测试账号登录商品中心
- 账号须具备组管理（规格/口味/做法/加料/套餐组）读写权限

## 数据命名

| 类型 | 格式 | 示例 |
|------|------|------|
| 新建组名 | `组名称-autocreate-{类型小写}-{正式编号}{场景后缀}` | `组名称-autocreate-spec-005b` |
| 新建明细名 | `{明细前缀}-autocreate-{类型}-{编号}` | `规格明细-autocreate-spec-004-01` |
| 数据依赖-单对象 | `TC_GRP_{类型}_{编号}` | `TC_GRP_SPEC_013` |
| 数据依赖-引用 | `{类型}_REF_{编号}` / 商品 `STD_REF_{编号}` | `TC_SPEC_GRP_REF_013` |
| 重复名校验 | 沿用正式用例反引号名 | `GRP_DUP`、`TASTE_DUP` |

- 执行前确认当前品牌下不存在将创建的同名的组/明细（或由平台 teardown 清理）

## 数据依赖用例（UI + 平台造数）

> 本文档 **仅含 UI 可执行用例**。接口造数/清理 **不写入本文档**，由执行计划完成。

- UI 用例写 **`数据标识`**、**`数据准备`**（一行指向执行计划）、**Readiness**、**`失败分类`**
- 步骤按固定组名/明细名/商品名查询定位，禁止「存在规格组 A」类模糊前置

## 导航入口

| 组类型 | 列表 | 新增 |
|--------|------|------|
| 规格组 | 商品管理 → 规格组 | 列表 → 新增 |
| 口味组 | 商品管理 → 口味组 | 列表 → 新增 |
| 做法组 | 商品管理 → 做法组 | 列表 → 新增 |
| 加料组 | 商品管理 → 加料组 | 列表 → 新增 |
| 套餐组 | 商品管理 → 套餐组 | 列表 → 新增（固定/组合搭配按用例） |

- 保存/确认按钮：组编辑页统一写「保存」或「确认」（与现网一致，以用例步骤为准）

## 步骤增强

- **禁止**「组名称或明细名称留空」「仅填组名称」「按需填写」等模糊步骤
- 必填项缺失：拆 `-A` 组名称为空 / `-B` 明细名称为空（单步骤仅测一项）
- 列表/查询：有已知关键字时用反引号具名
- 被引用/删除/同步类：写清 `数据标识` + Readiness

---


## 加料组管理

### 用例编号：TC-需求6-001

用例标题：加料组列表页展示正确

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组列表页。

测试步骤：
1. 查看查询区、列表区、新增入口及行内操作。

预期结果：
1. 查询区、列表区、新增入口展示正常。

---

### 用例编号：TC-需求6-002

用例标题：加料组查询与重置正确

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组列表页。

测试步骤：
1. 输入条件查询后重置。

预期结果：
1. 重置后恢复全部加料组列表。

---

### 用例编号：TC-需求6-003

用例标题：新增加料组无加料明细可保存成功

优先级：

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 「组名称」输入 `组名称-autocreate-add-003`。
2. 点击「添加加料商品」。
3. 「加料商品名称」输入 `明细-autocreate-add-003-01`。
4. 「标准价」输入 `1.00`（或选择有效加料商品）。
5. 点击「确认」保存明细。
6. 点击「保存」。
7. 返回列表，组名称查询框输入 `组名称-autocreate-add-003`，点击「查询」。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求6-004

用例标题：新增加料组选择加料商品保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`ADDON_01`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 加料组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `ADDON_01` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入加料组新增页。

测试步骤：
1. 「组名称」输入 `组名称-autocreate-add-004`。
2. 点击「添加加料商品」。
3. 「加料商品名称」输入 `明细-autocreate-add-004-01`。
4. 「标准价」输入 `1.00`（或选择有效加料商品）。
5. 点击「确认」保存明细。
6. 点击「保存」。
7. 返回列表，组名称查询框输入 `组名称-autocreate-add-004`，点击「查询」。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求6-005

用例标题：加料价格仅允许数字且最多两位小数

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 在加料明细价格输入字母、特殊字符或超过两位小数，保存。

预期结果：
1. 保存失败或失焦提示价格不合法；合法值为数字且最多两位小数。

---

### 用例编号：TC-需求6-006

用例标题：加料组名称品牌内重复保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 「组名称」输入 `GRP_DUP`。
2. 点击「添加加料商品」，「加料商品名称」输入 `明细-autocreate-add-006-01`。
3. 「标准价」输入 `1.00`（或选择有效加料商品）。
4. 点击「确认」保存明细。
5. 点击「保存」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，提示组名称不可重复。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求6-007

用例标题：加料份数最少大于最多时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 新增加料组，最少选择 3、最多选择 1，保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，提示最少须 ≤ 最多。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求6-008

用例标题：加料份数最少和最多均为0时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 最少、最多均填 0，保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求6-009

用例标题：加料份数最少0最多2时保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 最少 0、最多 2，补全其他必填项，保存。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求6-010

用例标题：默认可加购数量超过最多选择时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 设置最多选择 N，某明细默认可加购数量 > N，保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求6-011

用例标题：可加购数量小于最少选择时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 最少选择 2，明细可加购数量填 1，保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求6-012

用例标题：已被引用加料组新增明细不自动同步到商品

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_ADD_012`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 加料组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_ADD_012` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入加料组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_ADD_012`，点击「查询」。
2. 向已被引用的加料组新增加料商品明细，查看引用商品。

预期结果：
1. 引用商品不自动增加新明细。

---

### 用例编号：TC-需求6-013

用例标题：未被引用加料明细删除成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 删除组内未被引用的加料明细。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求6-014

用例标题：被商品引用的加料组删除失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 删除已被商品引用的加料组。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提示须先解除商品引用，删除失败。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求6-015

用例标题：解除引用后加料组可删除成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 在商品侧解除对加料组 D 的引用，再删除加料组 D。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求6-016

用例标题：编辑加料组后引用商品同步更新

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面出现组列表表格

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组编辑页。

测试步骤：
1. 修改被引用加料组的组名称并保存，查看商品侧。

预期结果：
1. 引用商品中加料组名称与编辑后一致。

---

### 用例编号：TC-需求6-017

用例标题：相同商品合并展示开关开启时C端合并展示（P1）

优先级：P1

执行类型：E2E

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 加料组添加同商品多规格明细，保存并下发到门店。
2. 在 C 端查看引用该加料组的商品加料选项。

预期结果：
1. C 端对同一商品合并展示；点选后可选择不同规格（以 xmind 步骤 10 为准）。

---

### 用例编号：TC-需求6-018

用例标题：被引用加料明细删除时弹出确认变更并同步影响关联商品

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`ADD_GRP_REF`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `ADD_GRP_REF` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入加料组新增页。

测试步骤：
1. 组名称查询框输入 `ADD_GRP_REF`，点击「查询」。
2. 在加料组编辑页删除加料明细 A。
3. 查看弹窗内容与变更预览。
4. 点击取消，再次删除并点击确认修改。
5. 打开引用该加料组的商品编辑页查看加料配置。

预期结果：
1. 弹出确认变更窗口，仅展示本属性组变更；标注将影响所有关联商品。
2. 变更预览展示更改前/后加料排序；删除项 A 标注影响所有关联商品。
3. 取消后加料明细 A 仍存在；确认后 A 从组内删除。

---


## 套餐组

### 用例编号：TC-需求6-019

用例标题：新增加料组必填项缺失保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 确认「组名称」输入框为空，不输入任何内容。
2. 点击「保存」或「确认」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 「组名称」字段出现必填校验提示。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求6-020

用例标题：加料份数最少和最多相同时保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 新增加料组，最少选择与最多选择均填 2，补全其他必填项，保存。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求6-021

用例标题：加料商品选择页交互验证正确

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_ADD_021`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_ADD_021` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入加料组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_ADD_021`，点击「查询」。
2. 新增加料组，打开加料商品选择弹窗。
3. 搜索商品、勾选、取消勾选后确认。

预期结果：
1. 弹窗支持搜索与多选。
2. 确认后组内仅含勾选的加料商品。

---

### 用例编号：TC-需求6-022

用例标题：编辑被引用加料明细后商品侧同步（价格除外）

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_ADD_022`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面出现组列表表格

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_ADD_022` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入加料组编辑页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_ADD_022`，点击「查询」，进入编辑页。
2. 编辑 D1 的可加购数量等非价格、非默认选中字段并保存。
3. 查看商品 P、Q 中该明细。

预期结果：
1. 引用商品中对应字段更新为编辑后的值。

---

### 用例编号：TC-需求6-023

用例标题：无加料明细的加料组可删除成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_ADD_023`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_ADD_023` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入加料组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_ADD_023`，点击「查询」。
2. 删除加料组 H 并确认。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求6-024

用例标题：新增加料组页点击取消不保存

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 填写组名称等字段，点击「取消」。
2. 列表搜索该组名称。

预期结果：
1. 未生成新加料组记录。

---

### 用例编号：TC-需求6-025

用例标题：加料组名称超长保存后截断为100字符

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 加料组名称输入超过 100 字符，保存。
2. 查看已保存组名称。

预期结果：
1. 保存成功，无报错拦截。
2. 组名称为前 100 个有效字符。

---

### 用例编号：TC-需求6-026

用例标题：相同商品合并不开启时C端分开展示

优先级：P1

执行类型：E2E

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入加料组新增页。

测试步骤：
1. 加料组添加同商品多规格明细，保存并下发。
2. 在 C 端查看引用该加料组的商品。

预期结果：
1. C 端同一商品按规格分开展示，不合并为一条。

---

### 用例编号：TC-需求6-027

用例标题：加料组名称仅大小写不同视为重复

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`AddOnGroup`
- extra_0：`addongroup`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `AddOnGroup` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入加料组新增页。

测试步骤：
1. 「组名称」输入 `addongroup`。
2. 点击「添加加料商品」，「加料商品名称」输入 `明细-autocreate-add-027-01`。
3. 「标准价」输入 `1.00`（或选择有效加料商品）。
4. 点击「确认」保存明细。
5. 点击「保存」。

预期结果：
1. 提交失败，页面提示包含 `SYSTEM-0002:参数冲突`。

---

### 用例编号：TC-需求6-028

用例标题：编辑被引用加料明细单次加价和默认选中后已引用商品不自动同步

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`ADD_SYNC_028`
- extra_0：`STD_ADD_SYNC_028`
- extra_1：`STD_ADD_SYNC_028`
- extra_2：`2.00`
- extra_3：`3.00`
- extra_4：`STD_ADD_SYNC_028`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面出现组列表表格

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `ADD_SYNC_028` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入加料组编辑页。

测试步骤：
1. 组名称查询框输入 `ADD_SYNC_028`，点击「查询」，进入编辑页。
2. 在加料组管理中将加料 A 设为默认选中，并将组内单次加价改为 `3.00`。
3. 点击「保存」。
4. 切换至商品管理 → 商品。
5. 商品名称查询框输入 `STD_ADD_SYNC_028`，点击「查询」，进入编辑页，查看该加料组。

预期结果：
1. 加料组管理保存成功，无报错拦截。
2. 商品内仍保留原商品侧覆盖结果，不因组侧修改而被改写。

---

### 用例编号：TC-需求6-029

用例标题：默认选中的加料子项在终端自动带入默认数量

优先级：P1

执行类型：E2E

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`ADD_DEFAULT_029`
- extra_0：`2`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `ADD_DEFAULT_029` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入加料组新增页。

测试步骤：
1. 组名称查询框输入 `ADD_DEFAULT_029`，点击「查询」。
2. 在终端打开引用该加料组的商品加料选择页。
3. 查看加料 A 的默认带入数量。

预期结果：
1. 加料 A 进入选择页时自动带入默认数量 `2`。

---

### 用例编号：TC-需求6-030

用例标题：未默认选中的加料子项在终端不自动加购

优先级：P1

执行类型：E2E

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`ADD_NODEFAULT_030`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `ADD_NODEFAULT_030` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入加料组新增页。

测试步骤：
1. 组名称查询框输入 `ADD_NODEFAULT_030`，点击「查询」。
2. 在终端打开引用该加料组的商品加料选择页。
3. 查看加料 B 的初始数量。

预期结果：
1. 加料 B 初始数量为 `0`，且处于未选中状态。

---

### 用例编号：TC-需求6-031

用例标题：加料子项展示顺序按后台顺序同步终端

优先级：P1

执行类型：E2E

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`ADD_ORDER_031`
- extra_0：`C → A → B`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `ADD_ORDER_031` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入加料组新增页。

测试步骤：
1. 组名称查询框输入 `ADD_ORDER_031`，点击「查询」。
2. 在终端打开引用该加料组的商品加料选择页。
3. 查看加料子项展示顺序。

预期结果：
1. 终端展示顺序与后台一致，为 `C → A → B`。

---


## S02 加料商品

### 用例编号：TC-需求6-032

用例标题：加料商品创建页不展示是否称重商品选项

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 配菜/加料
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入加料商品创建页。

测试步骤：
1. 查看商品价格区域。

预期结果：
1. 页面不展示是否称重商品选项。

---

### 用例编号：TC-需求6-033

用例标题：加料商品创建页不展示套餐组入口

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 配菜/加料
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入加料商品创建页。

测试步骤：
1. 查看页面中分组配置区域。

预期结果：
1. 页面不展示固定搭配或组合搭配套餐组入口。

---

### 用例编号：TC-需求6-034

用例标题：加料商品创建页不展示商品属性编辑区

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 配菜/加料
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入商品编辑页。

测试步骤：
1. 查看页面分区与配置区域。

预期结果：
1. 页面不展示商品属性编辑区，也不展示其中的口味、做法、加料及互斥入口。

---


## S07 加料商品规则

### 用例编号：TC-需求6-035

用例标题：加料商品被加料组引用且组被商品引用时不可删除

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`ADD_REF_027`
- addon_group：`ADD_GRP_027`
- std_product：`STD_REF_027`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品
- 就绪条件：页面出现商品列表表格

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `ADD_REF_027` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入商品列表页。

测试步骤：
1. 商品类型选择「配菜/加料」（若需筛选）。
2. 商品名称查询框输入 `ADD_REF_027`，点击「查询」。
3. 定位到该商品行，点击操作列「删除」。
4. 查看页面提示。
5. 再次按 `ADD_REF_027` 查询列表。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 页面提示需先解除加料组及商品引用关系后才能删除。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求6-036

用例标题：加料组新增时可搜索并选择该加料商品

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`ADD_SELECT_001`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 加料组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `ADD_SELECT_001` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入加料组管理页。

测试步骤：
1. 在添加商品弹窗中输入 `ADD_SELECT_001`。
2. 执行搜索 `ADD_SELECT_001`。

预期结果：
1. 搜索结果中可返回该加料商品。
2. 选择后该商品成功加入当前加料组。

---

### 用例编号：TC-需求6-037

用例标题：标准商品引用含该加料的加料组后加料商品不可删除

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`ADD_REF_034`
- addon_group：`ADD_GRP_034`
- std_product：`STD_REF_034`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品
- 就绪条件：页面出现商品列表表格

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `ADD_REF_034` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入商品列表页。

测试步骤：
1. 商品类型选择「配菜/加料」（若需筛选）。
2. 商品名称查询框输入 `ADD_REF_034`，点击「查询」。
3. 定位到该商品行，点击操作列「删除」。
4. 查看页面提示。
5. 再次按 `ADD_REF_034` 查询列表。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 页面提示需先解除引用关系后才能删除该加料商品。
3. 未产生不符合预期的数据变更。

---


## S08 商品内加料组

### 用例编号：TC-需求6-038

用例标题：标准商品内编辑加料组单次加价和默认选中仅对当前商品生效

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_STD_ADDON_088`
- extra_0：`TC_ADDON_GROUP_088`
- extra_1：`TC_ADDON_GROUP_088`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品
- 就绪条件：页面出现商品列表表格

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_STD_ADDON_088` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入商品编辑页。

测试步骤：
1. 商品名称查询框输入 `TC_STD_ADDON_088`，点击「查询」，进入编辑页。
2. 在商品属性中找到加料组 `TC_ADDON_GROUP_088`。
3. 将加料 B 设为默认选中，并将其商品内单次加价修改为 `2.00`。
4. 点击「保存」。
5. 返回列表，商品名称查询框输入 `TC_STD_ADDON_088`，点击「查询」，进入编辑页，查看加料组信息。
6. 进入加料组管理，打开 `TC_ADDON_GROUP_088` 查看组内原始配置。

预期结果：
1. 保存成功，无报错拦截。
2. 商品内加料组显示加料 B 为默认选中，单次加价为 `2.00`。
3. 加料组管理中的原始默认选中和单次加价配置不因本次商品内编辑而变化。

---

### 用例编号：TC-需求6-039

用例标题：标准商品内同一选项组仅允许一个默认选中子项

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_STD_ONE_DEFAULT_089`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品
- 就绪条件：页面出现商品列表表格

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_STD_ONE_DEFAULT_089` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入商品列表页。

测试步骤：
1. 商品名称查询框输入 `TC_STD_ONE_DEFAULT_089`，点击「查询」。
2. 在同一已引用选项组内依次将子项 A、子项 B 设为默认选中。
3. 查看同组选项当前默认选中状态。
4. 点击「保存」。
5. 返回列表，商品名称查询框输入 `TC_STD_ONE_DEFAULT_089`，点击「查询」，进入编辑页。

预期结果：
1. 同一组选项区域最终仅保留 1 个默认选中子项。
2. 保存后重新进入时，同一组选项内仍仅有 1 个默认选中子项。

---


## S07 补充


## S08 补充


## 未匹配自动化源（未写入正文）

| 用例编号 | 用例标题 |
|------|------|
| TC-需求6-040 | 加料组最少选择与最多选择同时为0保存失败 |
| TC-需求6-041 | 加料子项最小数量大于最大数量保存失败 |
| TC-需求6-042 | 加料组单次加价为0元时二次确认 |
| TC-需求6-043 | 组侧编辑加料单次加价不同步到已引用商品 |

> 以上条目在商品/组自动化源文件中无同名块，且未配置标题别名；需先在源文件生成后再提取。
