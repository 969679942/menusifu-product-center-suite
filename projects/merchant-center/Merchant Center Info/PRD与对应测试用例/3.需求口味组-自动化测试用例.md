# 3. 需求口味组-自动化测试用例

对应需求：[3.需求口味组.md](3.需求口味组.md)

| 章节 | 条数 |
|------|------|
| S05 口味组 | 22 |
| S08 商品内口味组 | 5 |

> 自动化块来源：`1.商品中心-商品管理-商品-自动化测试用例.md`、`2.商品中心-商品管理-组-自动化测试用例.md`；按**用例标题**匹配，不新增步骤。

---

# 商品中心-商品管理-组 自动化测试用例

> 由 PRD 需求测试用例按标题从 `2.商品中心-商品管理-组-自动化测试用例.md` 提取，不新增自动化步骤；规范见 [product-center-automation-test-case](.cursor/skills/product-center-automation-test-case/SKILL.md)。

## 组管理自动化套件约定

> 写入 `*-组-自动化测试用例.md` 顶部，**每条用例不再重复**。

## 登录与权限

- 使用执行计划绑定的测试账号登录商品中心
- 账号须具备组管理（规格/口味/做法/加料/套餐组）读写权限

## 数据命名

| 类型 | 格式 | 示例 |
|------|------|------|
| 新建组名 | `组名称-autocreate-{类型小写}-{正式编号}{场景后缀}` | `组名称-autocreate-spec-005b` |
| 新建明细名 | `{明细前缀}-autocreate-{类型}-{编号}` | `规格明细-autocreate-spec-004-01` |
| 数据依赖-单对象 | `TC_GRP_{类型}_{编号}` | `TC_GRP_SPEC_013` |
| 数据依赖-引用 | `{类型}_REF_{编号}` / 商品 `STD_REF_{编号}` | `TC_SPEC_GRP_REF_013` |
| 重复名校验 | 沿用正式用例反引号名 | `GRP_DUP`、`TASTE_DUP` |

- 执行前确认当前品牌下不存在将创建的同名的组/明细（或由平台 teardown 清理）

## 数据依赖用例（UI + 平台造数）

> 本文档 **仅含 UI 可执行用例**。接口造数/清理 **不写入本文档**，由执行计划完成。

- UI 用例写 **`数据标识`**、**`数据准备`**（一行指向执行计划）、**Readiness**、**`失败分类`**
- 步骤按固定组名/明细名/商品名查询定位，禁止「存在规格组 A」类模糊前置

## 导航入口

| 组类型 | 列表 | 新增 |
|--------|------|------|
| 规格组 | 商品管理 → 规格组 | 列表 → 新增 |
| 口味组 | 商品管理 → 口味组 | 列表 → 新增 |
| 做法组 | 商品管理 → 做法组 | 列表 → 新增 |
| 加料组 | 商品管理 → 加料组 | 列表 → 新增 |
| 套餐组 | 商品管理 → 套餐组 | 列表 → 新增（固定/组合搭配按用例） |

- 保存/确认按钮：组编辑页统一写「保存」或「确认」（与现网一致，以用例步骤为准）

## 步骤增强

- **禁止**「组名称或明细名称留空」「仅填组名称」「按需填写」等模糊步骤
- 必填项缺失：拆 `-A` 组名称为空 / `-B` 明细名称为空（单步骤仅测一项）
- 列表/查询：有已知关键字时用反引号具名
- 被引用/删除/同步类：写清 `数据标识` + Readiness

---


## S05 口味组

### 用例编号：TC-需求3-001

用例标题：口味组列表页展示正确

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入口味组列表页。

测试步骤：
1. 查看查询区、列表区、新增入口及列表字段布局。

预期结果：
1. 展示查询区、列表区、新增入口；口味组信息展示完整。

---

### 用例编号：TC-需求3-002

用例标题：口味组查询与重置正确

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入口味组列表页。

测试步骤：
1. 输入关键字查询，再点击「重置」。

预期结果：
1. 查询结果仅含匹配项；重置后条件清空并展示全部口味组。

---

### 用例编号：TC-需求3-003

用例标题：新增口味组仅填必填项保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 口味组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入口味组新增页。
3. 品牌内无 `TC_TASTE_GRP_001`；至少添加 1 条口味明细。

测试步骤：
1. 「组名称」输入 `TC_TASTE_GRP_001`。
2. 点击「添加口味明细」。
3. 「口味明细名称」输入 `口味明细-autocreate-taste-003-01`。
4. 「加价」输入 `0.00`（或保持默认）。
5. 点击「确认」保存明细。
6. 点击「保存」。
7. 返回列表，组名称查询框输入 `TC_TASTE_GRP_001`，点击「查询」。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求3-004-A

用例标题：新增口味组失败-组名称为空

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 口味组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入口味组新增页。

测试步骤：
1. 确认「组名称」输入框为空，不输入任何内容。
2. 点击「保存」或「确认」。

预期结果：
1. 页面仍停留在组新增/编辑页，未出现保存成功提示。
2. 「组名称」字段出现必填校验提示。
3. 未产生不完整组数据。

---

### 用例编号：TC-需求3-004-B

用例标题：新增口味组失败-口味明细名称为空

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 口味组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 重新从起始页面进入口味组新增页（不沿用上一场景表单状态）。
3. 当前品牌下不存在组 `组名称-autocreate-taste-004b`。

测试步骤：
1. 「组名称」输入 `组名称-autocreate-taste-004b`。
2. 点击「添加口味明细」或等价入口。
3. 「口味明细名称」保持为空，不输入。「加价」输入 `0.00`（或保持默认）。
4. 点击「确认」保存明细。
5. 点击「保存」保存组。

预期结果：
1. 页面仍停留在组新增/编辑页，未出现保存成功提示。
2. 「口味明细名称」字段出现必填校验提示。
3. 返回列表，组名称查询框输入 `组名称-autocreate-taste-004b`，点击「查询」，无本次新建记录。

---

### 用例编号：TC-需求3-005

用例标题：新增口味组无口味明细时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 口味组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入口味组新增页。

测试步骤：
1. 「组名称」输入 `组名称-autocreate-taste-005empty`。
2. 不添加任何组内明细/子项。
3. 点击「保存」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，页面提示须先查看并添加口味明细。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求3-006

用例标题：口味组名称品牌内重复保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TASTE_DUP`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TASTE_DUP` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入口味组新增页。

测试步骤：
1. 「组名称」输入 `TASTE_DUP`。
2. 点击「添加口味明细」，「口味明细名称」输入 `口味明细-autocreate-taste-006-01`。
3. 「加价」输入 `0.00`（或保持默认）。
4. 点击「确认」保存明细。
5. 点击「保存」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，提示组名称不可重复。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求3-007

用例标题：口味明细组内重名保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`微辣`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `微辣` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入口味组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_REF`，点击「查询」，进入编辑页。
2. 点击「添加口味明细」。
3. 「口味明细名称」输入 `微辣`。
4. 「加价」输入 `0.00`（或保持默认）。
5. 点击「确认」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，提示口味明细名称不可重复。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求3-008

用例标题：已被引用口味组新增明细不自动同步到商品

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_TASTE_008`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 口味组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_TASTE_008` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入口味组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_TASTE_008`，点击「查询」。
2. 向 T 新增口味明细并保存，查看引用商品。

预期结果：
1. 口味组含新明细；引用商品不自动增加该明细（或有提示）。

---

### 用例编号：TC-需求3-009

用例标题：未被引用的口味明细删除成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入口味组新增页。

测试步骤：
1. 删除明细 T2。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求3-010

用例标题：被引用的口味明细删除失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入口味组新增页。

测试步骤：
1. 删除 T1 并确认。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 删除失败，提示被商品使用不可删除。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求3-011

用例标题：被引用口味组删除失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入口味组新增页。

测试步骤：
1. 对已被商品引用的口味组执行删除。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 删除失败，提示已被商品引用。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求3-012

用例标题：编辑口味组后引用商品同步更新

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_TASTE_012`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面出现组列表表格

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_TASTE_012` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入口味组编辑页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_TASTE_012`，点击「查询」，进入编辑页。
2. 修改口味组名称/编码并保存，查看商品 P、Q。

预期结果：
1. 商品 P、Q 中口味组基础信息与编辑后一致。

---

### 用例编号：TC-需求3-013

用例标题：编辑被引用口味明细名称后商品同步

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面出现组列表表格

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入口味组编辑页。

测试步骤：
1. 修改口味名称（非价格、非默认选中）并保存。

预期结果：
1. 引用商品中该口味名称更新；价格/默认选中不在此用例验证范围。

---


## 做法组

### 用例编号：TC-需求3-014

用例标题：口味信息可从行业商品继承成功

优先级：P2

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入口味组新增页。

测试步骤：
1. 在口味组创建流程中选择从行业商品继承。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求3-015

用例标题：已有口味组新增明细缺必填项保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_TASTE_015`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 口味组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_TASTE_015` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入口味组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_TASTE_015`，点击「查询」。
2. 编辑口味组 A，添加口味明细但不填口味名称，确认保存。
3. 查看口味组 A。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求3-016

用例标题：解除引用后口味组可删除成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_TASTE_016`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_TASTE_016` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入口味组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_TASTE_016`，点击「查询」。
2. 在商品 A 中移除对口味组 T 的引用并保存。
3. 删除口味组 T。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求3-017

用例标题：新增口味组页点击取消不保存

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 口味组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入口味组新增页。

测试步骤：
1. 填写组名称等字段，点击「取消」。
2. 列表搜索该组名称。

预期结果：
1. 未生成新口味组记录。

---

### 用例编号：TC-需求3-018

用例标题：新增口味明细弹窗点击取消不保存

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_TASTE_019`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 口味组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_TASTE_019` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入口味组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_TASTE_019`，点击「查询」。
2. 编辑 A，添加口味明细并填写信息后点击「取消」。
3. 查看口味组 A 明细列表。

预期结果：
1. 仍只有明细 A1，无新增记录。

---

### 用例编号：TC-需求3-019

用例标题：口味明细名称超长保存后截断为100字符

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入口味组新增页。

测试步骤：
1. 口味明细名称输入超过 100 字符，保存。
2. 查看已保存名称。

预期结果：
1. 保存成功，无报错拦截。
2. 名称为前 100 个有效字符。

---

### 用例编号：TC-需求3-020

用例标题：口味组仅剩一个子项时删除该子项失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TASTE_SINGLE`
- extra_0：`T1`
- extra_1：`T1`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TASTE_SINGLE` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入口味组新增页。

测试步骤：
1. 组名称查询框输入 `TASTE_SINGLE`，点击「查询」。
2. 对明细 `T1` 执行删除操作。
3. 若弹出确认框则点击确认。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求3-021

用例标题：口味组名称仅大小写不同视为重复

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`FlavorGroup`
- extra_0：`flavorgroup`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `FlavorGroup` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入口味组新增页。

测试步骤：
1. 「组名称」输入 `flavorgroup`。
2. 点击「添加口味明细」，「口味明细名称」输入 `口味明细-autocreate-taste-022-01`。
3. 「加价」输入 `0.00`（或保持默认）。
4. 点击「确认」保存明细。
5. 点击「保存」。

预期结果：
1. 提交失败，页面提示包含 `SYSTEM-0002:参数冲突`。

---

### 用例编号：TC-需求3-022

用例标题：编辑被引用口味明细加价和默认选中后已引用商品不自动同步

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TASTE_SYNC_023`
- extra_0：`STD_TASTE_SYNC_023`
- extra_1：`STD_TASTE_SYNC_023`
- extra_2：`2.00`
- extra_3：`3.00`
- extra_4：`STD_TASTE_SYNC_023`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面出现组列表表格

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TASTE_SYNC_023` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入口味组编辑页。

测试步骤：
1. 组名称查询框输入 `TASTE_SYNC_023`，点击「查询」，进入编辑页。
2. 在口味组管理中将口味 A 设为默认选中，并将组内加价改为 `3.00`。
3. 点击「保存」。
4. 切换至商品管理 → 商品。
5. 商品名称查询框输入 `STD_TASTE_SYNC_023`，点击「查询」，进入编辑页，查看该口味组。

预期结果：
1. 口味组管理保存成功，无报错拦截。
2. 商品内仍保留原商品侧覆盖结果，不因组侧修改而被改写。

---


## S08 商品内口味组

### 用例编号：TC-需求3-023

用例标题：标准商品内编辑口味组加价和默认选中仅对当前商品生效

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_EDIT_ATTR_001`
- extra_0：`TC_FLAVOR_GROUP_001`
- extra_1：`TC_FLAVOR_GROUP_001`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品
- 就绪条件：页面出现商品列表表格

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_EDIT_ATTR_001` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入商品编辑页。

测试步骤：
1. 商品名称查询框输入 `TC_EDIT_ATTR_001`，点击「查询」，进入编辑页。
2. 在商品属性中找到口味组 `TC_FLAVOR_GROUP_001`。
3. 将口味 B 设置为默认选中，并将其商品内加价修改为 `2.00`。
4. 点击「保存」。
5. 返回列表，商品名称查询框输入 `TC_EDIT_ATTR_001`，点击「查询」，进入编辑页，查看口味组信息。
6. 进入口味组管理，打开 `TC_FLAVOR_GROUP_001` 查看组内原始配置。

预期结果：
1. 保存成功，无报错拦截。
2. 商品内口味组显示口味 B 为默认选中，加价为 `2.00`。
3. 口味组管理中的原始默认选中和加价配置不因本次商品内编辑而变化。

---

### 用例编号：TC-需求3-024

用例标题：继承到商品中的口味组基础信息变更后可同步到已关联商品

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_INHERIT_001`
- extra_0：`TC_INHERIT_FLAVOR_001`
- extra_1：`TC_INHERIT_FLAVOR_001`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 口味组
- 就绪条件：页面出现口味组列表

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_INHERIT_001` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入口味组管理页。

测试步骤：
1. 进入口味组管理，打开 `TC_INHERIT_FLAVOR_001`。
2. 修改口味组名称为 `TC_INHERIT_FLAVOR_001_V2` 并保存。
3. 在同步流程中确认将该变更同步到已关联商品。
4. 返回列表，商品名称查询框输入 `TC_INHERIT_001`，点击「查询」，进入编辑页。

预期结果：
1. 保存组信息后进入同步流程，已关联商品展示在同步范围内。
2. `TC_INHERIT_001` 中展示的口味组名称更新为 `TC_INHERIT_FLAVOR_001_V2`。

---

### 用例编号：TC-需求3-025

用例标题：商品内不可单独添加组子项仅可移除已引用子项

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 标准商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入标准商品创建页。
3. 商品已引用 1 个口味组或做法组。

测试步骤：
1. 查看已引用组选项区域。
2. 尝试直接新增组内单个子项。
3. 移除 1 个已引用子项。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。

---

### 用例编号：TC-需求3-026

用例标题：商品内移除已引用口味组子项后详情展示正确

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_STD_RMV_086`
- extra_0：`TC_RMV_FLAVOR_086`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品
- 就绪条件：页面出现商品列表表格

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_STD_RMV_086` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入商品列表页。

测试步骤：
1. 商品名称查询框输入 `TC_STD_RMV_086`，点击「查询」。
2. 从已引用口味组中移除子项 B。
3. 点击「保存」。
4. 返回列表，商品名称查询框输入 `TC_STD_RMV_086`，点击「查询」，进入编辑页，查看口味组展示。
5. 返回列表，商品名称查询框输入 `TC_STD_RMV_086`，点击「查询」，进入详情页，查看口味组展示。

预期结果：
1. 保存成功，无报错拦截。
2. 商品内仅展示口味 A、C，不再展示已移除的 B。

---

### 用例编号：TC-需求3-027

用例标题：标准商品内同一选项组仅允许一个默认选中子项

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_STD_ONE_DEFAULT_089`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品
- 就绪条件：页面出现商品列表表格

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_STD_ONE_DEFAULT_089` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入商品列表页。

测试步骤：
1. 商品名称查询框输入 `TC_STD_ONE_DEFAULT_089`，点击「查询」。
2. 在同一已引用选项组内依次将子项 A、子项 B 设为默认选中。
3. 查看同组选项当前默认选中状态。
4. 点击「保存」。
5. 返回列表，商品名称查询框输入 `TC_STD_ONE_DEFAULT_089`，点击「查询」，进入编辑页。

预期结果：
1. 同一组选项区域最终仅保留 1 个默认选中子项。
2. 保存后重新进入时，同一组选项内仍仅有 1 个默认选中子项。

---

