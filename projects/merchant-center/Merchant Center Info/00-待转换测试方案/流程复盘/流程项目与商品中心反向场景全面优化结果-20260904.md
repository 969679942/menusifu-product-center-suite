# 流程项目与商品中心反向场景全面优化结果

日期：2026-09-04  
范围：公共反向场景合同、流程项目静态用例、商品中心证据映射。  
结果类型：`public-core + project-adapter + domain-asset + generated-evidence`。

## 1. 执行方案

1. 公共层提供场景目录校验、场景用例注册和覆盖评估。
2. 流程项目按 FR-001～FR-026 建立合同型候选用例，绑定来源和 C-001～C-009 公共合同。
3. 商品中心只消费已有 runtime-audit V2 证据，建立证据候选映射，不把局部结果升级为公共完成。
4. 通过类型、指纹、上下文、证据、清理和授权门禁后，才允许后续增量重验。

## 2. 实际结果

| 范围 | 结果 |
|---|---|
| 流程场景目录 | 26/26 已登记 |
| 流程合同型候选用例 | 26/26 已建立，执行资格 0/26 |
| 流程场景覆盖 | covered 0、partial 26、missing 0 |
| 商品中心跨项目映射 | 10 个场景映射、5 个真实 caseId，全部 partial |
| 商品中心业务重跑 | 0 |
| 跨 applicationId 试点 | 0，保持 deferred |

## 3. 方案遗漏复核

- 未发现缺少来源、触发、证据、允许动作、公共合同或人工升级条件的场景目录项。
- 已补充 candidate 不得直接执行、场景重复映射去重、证据不完整保持 partial、外部触发不得绕过授权四项保护。
- 商品中心仍缺全方案级映射、execution grant 收据、完整事件流、全量 API 生命周期证据和真实跨系统试点；这些被保留为明确缺口。
- 流程项目仍缺真实项目适配器、运行收据、报告查询入口、RBAC 运营接入和 30 天遥测基线；没有将合同型候选误判为业务通过。

## 4. 验收与影响

- 公共 TypeScript、边界检查通过。
- 公共 API 合同 335/335 通过。
- 商品中心类型检查通过。
- 商品中心 runtime-audit 与平台适配合同 19/19 通过。
- 场景目录与候选用例注册校验通过。
- 既有通过结果未失效；本轮未启动业务页面、写请求、清理或跨系统执行。

## 5. 后续门禁

只有项目适配器补齐当前证据并生成标准收据后，才可将对应场景从 `partial` 转为 `covered`；任何业务规则冲突、危险写入、授权缺口、迁移漂移或跨系统条件不足，继续进入人工/平台异常队列。

## 6. 本轮仍需优化项的执行与复核

详细执行计划已固化为：
`deliverables/test-plan-governance/reverse-scenario-optimization-execution-v1.json`。

本轮实际执行：

- 已生成流程项目 26 个场景的适配器就绪清单，逐项列出 caseId、所需证据、缺口和下一动作；所有候选用例仍不可执行。
- 已增加商品中心反向场景映射校验，确认 10 个场景映射、5 个 caseId 均存在于 runtime-audit 证据，且状态全部保持 `partial`。
- 已运行流程覆盖评估，结果仍为 `0 covered / 26 partial / 0 missing`。
- 已运行公共与项目类型检查及反向场景合同校验；未启动业务页面、CRUD、清理或跨 applicationId 试点。

影响复核：新增内容均为 `generated-evidence` 或静态 `project-adapter` 校验，不改变既有用例状态、历史收据和已通过结果；只读校验失败时阻断报告生成，不会写入业务数据。

遗漏复核：仍缺真实运行收据、execution grant、完整事件流、全方案 API/UI 清理证据、流程运营查询入口和 30 天遥测；这些缺口已显式标记为 deferred 或 blocked-by-runtime-evidence，不得通过静态文件自动补齐。
