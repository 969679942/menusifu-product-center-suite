# 已落地自动化用例

本目录只保存已有独立可执行自动化绑定的用例索引，不保存测试用例正文、来源资料、脚本或运行报告。

- 权威用例正文位于 `../用例库/`。
- 自动化脚本位于 `Merchant Center UITest/`。
- 运行证据位于 `deliverables/` 或 `Merchant Center UITest/output/`。
- `index.md` 和 `index.json` 由资产治理命令生成。
