# 商品中心业务规则快速晋级工作台

> 本文件由程序生成，只用于评审和审计；唯一业务事实源仍是 `../商品中心业务规则.md`。

## 当前结论

- 文档规则：160；正式业务规则：6；待生命周期核验：148。
- 已完成静态预审且可批量审核：20 条，共 4 个语义包。
- 其中当前执行已验证：0 条；批准后仍需最小验证：20 条。
- 真正需要逐条业务裁决：0 条；时间/上下文需要人工确认：0 条。
- 剩余 128 条由系统继续补来源、匹配用例或关联收据，不交给人工逐条处理。
- 用例派生候选 225 条已压缩为 60 个语义簇；它们不等于正式规则。

## 你需要审核的批量规则包

审核时只判断规则中文表述是否是当前业务口径。执行时间、环境、租户、角色、语言、路由、指纹和收据由系统处理。

### document-rule-approval-f5d72f252907

- 模块：2.2 全局格式与输入（B 端规范）
- 状态：ready-for-human-batch-approval
- 审核范围：仅审核规则语义、作用域及与既有正式规则的包含关系；时间、上下文和运行验证由系统自动处理。

| 规则 | 当前业务表述 | 来源 | 执行验证 | 批准后的处理 |
|---|---|---|---|---|
| BR-FMT-002 | 助记码、规格值、设备编码：最长 20 字符。 | B端（第 1926 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-FMT-005 | 价格输入 0.00 可保存；界面默认展示 2 位小数；输入超过 2 位小数时按四舍五入保留 2 位后保存，API 数值与重新进入页面的回显均为舍入后的金额（Q-ITEM-003）。 | 现网+人工确认（第 1929 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-FMT-009 | 查询或重置后列表回到第 1 页；筛选项变更后自动刷新列表；重置后查询条件清空、恢复全量结果并回到第 1 页。 | 现网（第 1933 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |

### document-rule-approval-bb02236a24e9

- 模块：规格组专有
- 状态：ready-for-human-batch-approval
- 审核范围：仅审核规则语义、作用域及与既有正式规则的包含关系；时间、上下文和运行验证由系统自动处理。

| 规则 | 当前业务表述 | 来源 | 执行验证 | 批准后的处理 |
|---|---|---|---|---|
| BR-GRP-SPEC-001 | 规格组保存前须至少一个规格子项（见 BR-GRP-001）。 | B端（第 1993 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-SPEC-002 | 规格子项名称 品牌内不可重复（见 BR-GRP-003）。 | B端（第 1994 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |

### document-rule-approval-1484ca2864a3

- 模块：组共用规则（规格 / 口味 / 做法 / 加料 / 套餐）
- 状态：ready-for-human-batch-approval
- 审核范围：仅审核规则语义、作用域及与既有正式规则的包含关系；时间、上下文和运行验证由系统自动处理。

| 规则 | 当前业务表述 | 来源 | 执行验证 | 批准后的处理 |
|---|---|---|---|---|
| BR-GRP-001 | 组保存前须至少存在一个有效子项。加料组无加料明细时「确定」按钮置灰且不发送创建请求；固定搭配、可选搭配和随心配无商品时提交显示「至少有一个子项」，停留在新增页且不发送创建请求。 | 现网、B端（第 1980 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-002 | 组名称：100 字符，品牌内组名称不可重复（英文字母不区分大小写），必填；重复提示 SYSTEM-0002:参数冲突。套餐组名称中空格计入 100 字符长度，首尾空格不能形成有效保存值。 | B端（第 1981 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-003 | 子项名称：100 字符，组内不可重复；规格子项品牌内不可重复。 | B端（第 1982 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-004 | 规格设备编码最长 20 字符；规格值最长 20 字符。 | 现网（第 1983 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-031 | 规格组未被商品引用时新增规格子项直接保存到组。规格、口味、做法和加料组已被商品引用时，组内新增子项保存到组，但不自动应用到既有引用商品。可选搭配已被套餐商品引用时，新增套餐商品后的保存确认弹窗展示引用套餐商品数量和影响明细，确认后同步到全部引用套餐商品。 | 现网+人工确认（第 1985 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-032 | 组基础信息编辑后先保存到组；组已被商品使用时，非价格/默认选中变更影响引用商品并进入影响确认。确认弹窗展示更改前后值、受影响商品总数和默认选中数量 N/N，确认后同步所有已选择的引用商品。 | 现网+人工确认（第 1986 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-033 | 组内子项编辑后先保存到组；组已被商品使用时，非价格、非默认选中字段同步到引用该子项的商品，组侧价格和默认选中不覆盖商品侧独立配置。 | PRD、现网（第 1987 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-034 | 规格组被商品引用时删除失败，解除引用后可删除；口味组被引用时可经确认删除并清除商品中的无效引用；未被引用的做法组可经二次确认删除。套餐组内含商品明细不构成删除阻断，未被套餐商品引用时可直接删除，并同步删除组内明细。 | 现网+人工确认（第 1988 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-035 | 组内子项删除：未被引用且组内存在多个选项 → 可经确认变更删除；仅剩一个选项时不可删除并提示「该组只有一个选项，不能删除」。规格 被引用 → 删除失败，须先从商品移除。口味/做法/加料 被引用 → 走确认变更弹窗（变更预览、影响所有关联商品），确认后同步移除。套餐组仅剩一个套餐商品时同样不可删除；存在多个套餐商品时可删除，已被套餐商品引用则确认后同步更新。 | PRD+现网+人工确认（第 1989 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |

### document-rule-approval-76d0bd89181f

- 模块：A.3 口味/做法/加料/套餐组 BR
- 状态：ready-for-human-batch-approval
- 审核范围：仅审核规则语义、作用域及与既有正式规则的包含关系；时间、上下文和运行验证由系统自动处理。

| 规则 | 当前业务表述 | 来源 | 执行验证 | 批准后的处理 |
|---|---|---|---|---|
| BR-GRP-005 | 口味组/做法组/加料组/套餐组：保存时须至少一个子项；仅剩一个子项时不可删除，提示「该组只有一个选项，不能删除」（以现网为准）。口味、做法子项名称必填；做法组默认空行的做法名称为空时提示「做法名称（必填）」。 | 现网+人工确认（第 2000 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-010 | 固定搭配：组内商品和数量固定，商品行配置数量，由外层套餐商品统一定价；无组级选择数量。 | 现网（第 2003 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-011 | 可选搭配（旧称组合搭配）：组级配置必填选择数量；商品行展示并保存加价和默认选中；页面提供可独立开关的组内重复选择和相同商品合并配置。 | 现网（第 2004 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-012 | 随心配：按总数量配置最少/最多选择；商品行支持原价、默认/自定义价格来源、自定义价格和默认选中，不存在默认数量字段；开启重复选择后可限制子项最大数量。 | 现网+人工确认（第 2007 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-013 | 新增套餐组时允许在固定搭配、可选搭配、随心配之间切换，商品列表和类型专属字段随当前类型切换；点击确定后仅保存当前选择的一种套餐类型。套餐组保存后类型锁定，编辑页不可切换类型。 | 现网复核 2026-09-04（第 2008 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |
| BR-GRP-020 | 数量规则：加料组使用组级最少选择份数、最多选择份数和份数内免费，商品行使用最小数量、最大数量；组级最少/最多允许同时为 0，但组内须存在至少一个有效加料商品。可选搭配使用单一选择数量和逐商品默认选中开关；随心配使用组级最少/最多总数量，最多数量必须 >= 最少数量，否则提示 最多选择数量不能小于最少选择数量。随心配最少/最多输入非数字时自动清空，输入负数或 0 时自动更正为 1，输入小数时忽略小数部分。可选搭配和随心配的重复选择开关控制子项数量字段。 | 现网+人工确认（第 2001 行） | verification-pending | 批准后登记为正式规则，但验证状态保持待验证；仅生成最小增量验证候选，不自动执行。 |

## 仅有的逐条业务冲突

当前无业务冲突。

## 系统自动处理队列

- 文档规则：来源修复 22、用例设计 69、收据关联 37。
- 用例候选：来源修复 217、证据补齐 0、测试合同分流 0、已被正式规则覆盖 0、延期保持 8。
- 上述项目不需要业务人员确认；处理完成后才会生成新的批量审核包。

## 数字口径

- “用例级确认决定”当前为 54，它是历史用例整改决定，绝不再显示为正式业务规则数。
- 规则语义批准与执行验证分离：允许先确认规则是什么，再通过最小用例集合补验证；没有完整收据仍不能把验证状态写成通过。
- 本次生成没有执行 UI/API、没有修改正式规则、没有使历史结果失效。
