# 组方案产品偏差清单

当前共 **1 条** 未关闭 `product-defect`。

- `TC-GRP-PKG-045`：2026-09-05 当前标准执行收据显示，已保存套餐组编辑页三个类型单选项均禁用但均未选中，违反“仅一个选中且全部禁用”的正式预期；已登记为当前产品偏差，待产品修复或业务规则确认后重跑。详见 `TC-GRP-PKG-045-已保存套餐组类型无选中状态.md`。

- `TC-GRP-PKG-030`：2026-09-03 当前 SaaS 黑盒复核已验证 `3/1` 输入、中文精确提示、拒绝提交、API 零落库及 UI/API 零残留，状态为 `resolved`，已恢复正常自动化绑定。详见 `TC-GRP-PKG-030-中文校验提示与实际英文不一致.md`。

2026-08-19 第二批加料与套餐 8 条偏差已依据人工确认规则完成重基线：

- 6 条转为当前有效业务行为并恢复自动化绑定。
- `TC-GRP-PKG-025`、`TC-GRP-PKG-032` 确认为历史不存在字段，不再作为产品缺陷。
- 同源的 `TC-GRP-PKG-015` 同步标记为 `not-applicable`，防止旧默认数量规则从其他入口重新生成。

详见：`已确认业务规则-20260819-加料套餐第二批.md`。

后续新偏差必须同时具备当前正式预期、页面或 API 运行证据及用例指纹，才能加入本清单。
