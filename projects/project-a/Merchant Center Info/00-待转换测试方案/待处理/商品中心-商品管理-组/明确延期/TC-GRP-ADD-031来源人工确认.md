# TC-GRP-ADD-031 来源人工确认

- 用例：`TC-GRP-ADD-031 加料子项展示顺序按后台顺序同步终端`
- 当前处置：保留并跳过；正式用例已恢复，本轮不进入自动执行。
- 已有来源：原始 XMind 仅明确后台加料明细按排列顺序展示。
- 缺少来源：未找到“后台顺序必须同步到终端”的明确 PRD、XMind 或业务规则。
- 环境限制：当前还缺少真实终端/C 端状态观测与同步证据。

## 当前用例

前置条件：
1. 加料组 `ADD_ORDER_031` 已配置加料 A、B、C。
2. 后台中已将展示顺序调整为 `C → A → B`。
3. 已有商品引用该加料组并完成下发。

测试步骤：
1. 在终端打开引用该加料组的商品加料选择页。
2. 查看加料子项展示顺序。

预期结果：
1. 终端加料选择页打开并加载 3 条子项。
2. 子项顺序严格为 `C → A → B`。

## 人工确认结果

- 处置：保留并跳过
- 业务规则是否确认“后台加料子项顺序同步到终端”：暂不确认，待终端能力接入后再验证
- 若确认，请填写规则或需求来源：不适用
- 若不确认，是否改为仅验证后台列表顺序：本轮不改写，保留原 C 端验证场景
- 审核说明：当前缺少真实终端/C 端状态观测与同步证据，后续接入能力后恢复执行
