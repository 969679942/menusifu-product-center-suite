# 2. 需求规格组-自动化测试用例

对应需求：[2.需求规格组.md](2.需求规格组.md)

| 章节 | 条数 |
|------|------|
| S05 规格组 | 28 |
| S02 商品内多规格 | 11 |

> 自动化块来源：`1.商品中心-商品管理-商品-自动化测试用例.md`、`2.商品中心-商品管理-组-自动化测试用例.md`；按**用例标题**匹配，不新增步骤。

---

# 商品中心-商品管理-组 自动化测试用例

> 由 PRD 需求测试用例按标题从 `2.商品中心-商品管理-组-自动化测试用例.md` 提取，不新增自动化步骤；规范见 [product-center-automation-test-case](.cursor/skills/product-center-automation-test-case/SKILL.md)。

## 组管理自动化套件约定

> 写入 `*-组-自动化测试用例.md` 顶部，**每条用例不再重复**。

## 登录与权限

- 使用执行计划绑定的测试账号登录商品中心
- 账号须具备组管理（规格/口味/做法/加料/套餐组）读写权限

## 数据命名

| 类型 | 格式 | 示例 |
|------|------|------|
| 新建组名 | `组名称-autocreate-{类型小写}-{正式编号}{场景后缀}` | `组名称-autocreate-spec-005b` |
| 新建明细名 | `{明细前缀}-autocreate-{类型}-{编号}` | `规格明细-autocreate-spec-004-01` |
| 数据依赖-单对象 | `TC_GRP_{类型}_{编号}` | `TC_GRP_SPEC_013` |
| 数据依赖-引用 | `{类型}_REF_{编号}` / 商品 `STD_REF_{编号}` | `TC_SPEC_GRP_REF_013` |
| 重复名校验 | 沿用正式用例反引号名 | `GRP_DUP`、`TASTE_DUP` |

- 执行前确认当前品牌下不存在将创建的同名的组/明细（或由平台 teardown 清理）

## 数据依赖用例（UI + 平台造数）

> 本文档 **仅含 UI 可执行用例**。接口造数/清理 **不写入本文档**，由执行计划完成。

- UI 用例写 **`数据标识`**、**`数据准备`**（一行指向执行计划）、**Readiness**、**`失败分类`**
- 步骤按固定组名/明细名/商品名查询定位，禁止「存在规格组 A」类模糊前置

## 导航入口

| 组类型 | 列表 | 新增 |
|--------|------|------|
| 规格组 | 商品管理 → 规格组 | 列表 → 新增 |
| 口味组 | 商品管理 → 口味组 | 列表 → 新增 |
| 做法组 | 商品管理 → 做法组 | 列表 → 新增 |
| 加料组 | 商品管理 → 加料组 | 列表 → 新增 |
| 套餐组 | 商品管理 → 套餐组 | 列表 → 新增（固定/组合搭配按用例） |

- 保存/确认按钮：组编辑页统一写「保存」或「确认」（与现网一致，以用例步骤为准）

## 步骤增强

- **禁止**「组名称或明细名称留空」「仅填组名称」「按需填写」等模糊步骤
- 必填项缺失：拆 `-A` 组名称为空 / `-B` 明细名称为空（单步骤仅测一项）
- 列表/查询：有已知关键字时用反引号具名
- 被引用/删除/同步类：写清 `数据标识` + Readiness

---


## S05 规格组

### 用例编号：TC-需求2-001

用例标题：规格组列表页展示正确

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_SPEC_001`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_SPEC_001` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组列表页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_SPEC_001`，点击「查询」。
2. 查看查询区、列表区、新增入口及行内操作。

预期结果：
1. 页面展示查询区、列表区、新增按钮及编辑/删除等操作入口。
2. 列表中规格组名称、关联信息等字段展示完整，无错位、无异常文案。

---

### 用例编号：TC-需求2-002

用例标题：规格组按名称模糊查询成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_SPEC_002`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_SPEC_002` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组列表页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_SPEC_002`，点击「查询」。
2. 点击「重置」。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 重置后查询条件清空，列表恢复为全部规格组（回首页）。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求2-003

用例标题：规格组按第二语言多语言查询成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`スペック`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `スペック` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组列表页。

测试步骤：
1. 组名称查询框输入 `スペック`，点击「查询」。
2. 在查询框输入 `スペック`，点击「查询」。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求2-004

用例标题：新增规格组仅填必填项保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入规格组新增页。
3. 品牌内不存在同名规格组 `TC_SPEC_GRP_001`。

测试步骤：
1. 「组名称」输入 `TC_SPEC_GRP_001`。
2. 点击「添加规格明细」。
3. 「规格明细名称」输入 `规格明细-autocreate-spec-004-01`。
4. 不填写其他非必填项（排序等保持默认）。
5. 点击「确认」保存明细。
6. 点击「保存」。
7. 返回列表，组名称查询框输入 `TC_SPEC_GRP_001`，点击「查询」。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表可搜到 `TC_SPEC_GRP_001`，且含所填规格明细。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求2-005-A

用例标题：新增规格组失败-组名称为空

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入规格组新增页。

测试步骤：
1. 确认「组名称」输入框为空，不输入任何内容。
2. 点击「保存」或「确认」。

预期结果：
1. 页面仍停留在组新增/编辑页，未出现保存成功提示。
2. 「组名称」字段出现必填校验提示。
3. 未产生不完整组数据。

---

### 用例编号：TC-需求2-005-B

用例标题：新增规格组失败-规格明细名称为空

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 重新从起始页面进入规格组新增页（不沿用上一场景表单状态）。
3. 当前品牌下不存在组 `组名称-autocreate-spec-005b`。

测试步骤：
1. 「组名称」输入 `组名称-autocreate-spec-005b`。
2. 点击「添加规格明细」或等价入口。
3. 「规格明细名称」保持为空，不输入。不填写其他非必填项（排序等保持默认）。
4. 点击「确认」保存明细。
5. 点击「保存」保存组。

预期结果：
1. 页面仍停留在组新增/编辑页，未出现保存成功提示。
2. 「规格明细名称」字段出现必填校验提示。
3. 返回列表，组名称查询框输入 `组名称-autocreate-spec-005b`，点击「查询」，无本次新建记录。

---

### 用例编号：TC-需求2-006

用例标题：新增规格组无规格明细保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入规格组新增页。

测试步骤：
1. 「组名称」输入 `TC_SPEC_EMPTY`。
2. 不添加任何组内明细/子项。
3. 点击「保存」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，页面提示「请先添加子项再保存」。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求2-007

用例标题：已有规格组新增明细缺必填项保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_SPEC_007`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_SPEC_007` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_SPEC_007`，点击「查询」。
2. 编辑规格组 A，点击添加规格。
3. 不填规格名称，填写其他字段后确认。
4. 返回查看规格组 A。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求2-008

用例标题：规格组名称品牌内重复保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`GRP_DUP`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `GRP_DUP` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组新增页。

测试步骤：
1. 「组名称」输入 `GRP_DUP`。
2. 点击「添加规格明细」，「规格明细名称」输入 `规格明细-autocreate-spec-008-01`。
3. 不填写其他非必填项（排序等保持默认）。
4. 点击「确认」保存明细。
5. 点击「保存」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，提示组名称不可重复。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求2-009

用例标题：规格明细组内重名保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`SIZE_M`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `SIZE_M` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_REF`，点击「查询」，进入编辑页。
2. 点击「添加规格明细」。
3. 「规格明细名称」输入 `SIZE_M`。
4. 不填写其他非必填项（排序等保持默认）。
5. 点击「确认」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，提示规格明细名称不可重复（组内）。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求2-010

用例标题：规格名称超长保存后截断为100字符

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入规格组新增页。

测试步骤：
1. 组名称输入超过 100 字符（含特殊字符、首尾空格），保存。
2. 查看已保存的组名称。

预期结果：
1. 保存成功或失焦时按规则处理。
2. 最终保存的组名称为前 100 个有效字符，首尾空格已去除。

---

### 用例编号：TC-需求2-011

用例标题：规格设备编码超过20字符按规则处理

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入规格组新增页。

测试步骤：
1. 设备编码输入超过 20 字符，保存规格明细。

预期结果：
1. 超长部分被截断或失焦飘红提示，最终保存值不超过 20 字符。

---

### 用例编号：TC-需求2-012

用例标题：规格组名称仅大小写不同视为重复

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`SpecGroup`
- extra_0：`specgroup`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `SpecGroup` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组新增页。

测试步骤：
1. 「组名称」输入 `specgroup`。
2. 点击「添加规格明细」，「规格明细名称」输入 `规格明细-autocreate-spec-012-01`。
3. 不填写其他非必填项（排序等保持默认）。
4. 点击「确认」保存明细。
5. 点击「保存」。

预期结果：
1. 提交失败，页面提示包含 `SYSTEM-0002:参数冲突`。

---

### 用例编号：TC-需求2-013

用例标题：已被商品引用的组新增子项不自动同步到商品

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_SPEC_013`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_SPEC_013` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_SPEC_013`，点击「查询」。
2. 编辑规格组 A，新增规格明细 A2 并保存。
3. 打开商品 P 的规格引用信息。

预期结果：
1. 规格组 A 列表/详情含 A1、A2。
2. 商品 P 仍只引用原明细集合（不含 A2）。
3. 页面出现「不自动应用到已引用商品」提示。

---

### 用例编号：TC-需求2-014

用例标题：未被引用的组新增子项保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入规格组新增页。

测试步骤：
1. 「组名称」输入 `组名称-autocreate-spec-014`。
2. 点击「添加规格明细」。
3. 「规格明细名称」输入 `规格明细-autocreate-spec-014-01`。
4. 不填写其他非必填项（排序等保持默认）。
5. 点击「确认」保存明细。
6. 点击「保存」。
7. 返回列表，组名称查询框输入 `组名称-autocreate-spec-014`，点击「查询」。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求2-015

用例标题：未被引用的规格明细删除成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_SPEC_015`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_SPEC_015` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_SPEC_015`，点击「查询」。
2. 编辑规格组 G，对明细 G2 执行删除并确认。
3. 再次查看规格组 G。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. G2 从规格组 G 中移除；G1 仍存在。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求2-016

用例标题：被商品引用的规格明细删除失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_SPEC_016`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_SPEC_016` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_SPEC_016`，点击「查询」。
2. 编辑规格组 G，对明细 G1 执行删除并确认。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 删除失败，提示该规格明细被商品使用不可删除。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求2-017

用例标题：被商品引用的规格组删除失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_SPEC_017`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_SPEC_017` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_SPEC_017`，点击「查询」。
2. 在规格组列表对 G 点击「删除」并确认。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 删除失败，提示规格组被商品引用不可删除。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求2-018

用例标题：解除引用后规格组可删除成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入规格组新增页。

测试步骤：
1. 在商品 A 中移除对规格组 G / 明细 G1 的引用并保存。
2. 删除规格组 G 下全部未引用明细后，删除规格组 G。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求2-019

用例标题：无子项的规格组可删除成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入规格组新增页。

测试步骤：
1. 删除规格组 H 并确认。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表中不再展示 H（逻辑删除）。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求2-020

用例标题：编辑规格组信息后引用商品同步更新

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_SPEC_020`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面出现组列表表格

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_SPEC_020` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组编辑页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_SPEC_020`，点击「查询」，进入编辑页。
2. 编辑规格组 A，修改组名称/编码等基础信息并保存。
3. 分别查看商品 P、Q 中引用的规格组信息。

预期结果：
1. 规格组 A 列表展示为修改后的名称/编码。
2. 商品 P、Q 中引用的规格组基础信息与修改后一致。

---

### 用例编号：TC-需求2-021

用例标题：编辑被引用规格明细后商品侧同步（价格除外）

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_SPEC_021`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面出现组列表表格

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_SPEC_021` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组编辑页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_SPEC_021`，点击「查询」，进入编辑页。
2. 编辑明细 S1 的规格名称（非价格、非默认选中字段）并保存。
3. 查看商品 P、Q 中该规格明细展示。

预期结果：
1. 商品 P、Q 中对应规格名称更新为编辑后的值。

---

### 用例编号：TC-需求2-022

用例标题：新增规格组页点击「取消」不保存

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入规格组新增页。

测试步骤：
1. 填写组名称等字段，点击「取消」。
2. 列表搜索该组名称。

预期结果：
1. 未生成新规格组记录。

---

### 用例编号：TC-需求2-023

用例标题：新增规格明细弹窗点击「取消」不保存

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_SPEC_023`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_SPEC_023` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_SPEC_023`，点击「查询」。
2. 编辑 A，点击添加规格，填写信息后点击「取消」。
3. 查看规格组 A 明细列表。

预期结果：
1. 仍只有明细 A1，无新增记录。

---

### 用例编号：TC-需求2-024

用例标题：规格信息可从行业商品继承成功

优先级：P2

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入规格组新增页。

测试步骤：
1. 在规格组创建流程中选择从行业商品继承规格信息。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---


## 口味组

### 用例编号：TC-需求2-025

用例标题：规格子项名称品牌内重复保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`SIZE_L`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `SIZE_L` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入规格组新增页。

测试步骤：
1. 组名称查询框输入 `SIZE_L`，点击「查询」。
2. 新增规格组并添加规格明细名称为 `SIZE_L`，保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，提示规格名称品牌内不可重复。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求2-026

用例标题：规格明细名称超长保存后截断为100字符

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入规格组新增页。

测试步骤：
1. 规格明细名称输入超过 100 字符（含特殊字符），保存。
2. 查看已保存的规格明细名称。

预期结果：
1. 保存成功，无报错拦截。
2. 规格明细名称为前 100 个有效字符。

---

### 用例编号：TC-需求2-027

用例标题：规格值超过20字符按规则处理

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入规格组新增页。

测试步骤：
1. 规格值输入超过 20 字符，保存。

预期结果：
1. 超长被截断或失焦提示，最终保存值不超过 20 字符。

---

### 用例编号：TC-需求2-028

用例标题：新增规格组填写全部字段保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 规格组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入规格组新增页。

测试步骤：
1. 「组名称」输入 `组名称-autocreate-spec-028`。
2. 点击「添加规格明细」。
3. 「规格明细名称」输入 `规格明细-autocreate-spec-028-01`。
4. 不填写其他非必填项（排序等保持默认）。
5. 点击「确认」保存明细。
6. 点击「保存」。
7. 返回列表，组名称查询框输入 `组名称-autocreate-spec-028`，点击「查询」。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表展示新建规格组及全部已填字段。
3. 关键字段与步骤输入一致。

---


## S02 商品内多规格

### 用例编号：TC-需求2-029

用例标题：加料商品创建页不展示多规格入口

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 配菜/加料
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入加料商品创建页。

测试步骤：
1. 查看商品价格区域。

预期结果：
1. 页面不展示多规格入口。

---

### 用例编号：TC-需求2-030

用例标题：套餐商品创建页不展示多规格与称重相关入口

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。

测试步骤：
1. 查看商品价格区域。

预期结果：
1. 页面不展示多规格入口。
2. 页面不展示是否称重商品入口。

---

### 用例编号：TC-需求2-033

用例标题：从行业商品库选择多规格商品时可继承多规格及图库信息

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_DATA_STD_026`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 标准商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_DATA_STD_026` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入标准商品创建页。

测试步骤：
1. 点击「从行业商品库选择」。
2. 搜索商品 A 并点击「选择」。
3. 查看新建商品页面已继承的信息。
4. 补充商品价格并点击「保存」。
5. 查看规格、口味及图库信息。

预期结果：
1. 新建页面继承行业商品的名称、规格、口味、图片等信息，行业名称字段置灰不可编辑。
2. 保存成功，商品添加到选择的分类下。
3. 规格、口味相关页签展示继承的组信息。

---

### 用例编号：TC-需求2-034

用例标题：从行业商品库选择多规格商品时仅勾选部分规格可成功继承所选规格

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_DATA_STD_027`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 标准商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_DATA_STD_027` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入标准商品创建页。

测试步骤：
1. 点击「从行业商品库选择」。
2. 搜索商品 A，选择规格 1 和规格 2，不选择规格 3。
3. 查看新建商品页面已继承的信息。
4. 补充商品价格并点击「保存」。
5. 查看规格、口味及图库信息。

预期结果：
1. 新建页面继承行业商品的基础信息、图片和组选项信息。
2. 保存成功，商品添加到选择的分类下。
3. 规格页签中仅展示规格 1 和规格 2，不展示规格 3。

---

### 用例编号：TC-需求2-035

用例标题：多规格商品选择已有规格组后创建成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 标准商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入标准商品创建页。
3. 当前品牌下不存在测试商品 `TC_STD_047`。

测试步骤：
1. 商品名称填写 `TC_STD_047`。
2. 起售数量填写 `1`。
3. 商品价格选择多规格。
4. 选择规格组 `SPEC_GRP_001` 并填写各规格价格。
5. 点击「保存」。
6. 返回列表，商品名称查询框输入 `TC_STD_047`，点击「查询」。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表按 `TC_STD_047` 查询有且仅有 1 条记录。
3. 列表可搜索到 `TC_STD_047`，且详情中展示已引用的规格组。

---

### 用例编号：TC-需求2-036

用例标题：多规格商品点击去创建可跳转规格组新增页

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 标准商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入标准商品创建页。

测试步骤：
1. 在商品价格区域选择多规格。
2. 点击规格组旁的“去创建”入口。

预期结果：
1. 成功跳转到规格组新增页。

---

### 用例编号：TC-需求2-037

用例标题：选择多规格后是否称重商品置灰不可选

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 标准商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入标准商品创建页。

测试步骤：
1. 在商品价格区域选择多规格。
2. 查看是否称重商品选项状态。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。

---

### 用例编号：TC-需求2-038

用例标题：多规格商品默认规格下发后终端点餐默认选中该规格

优先级：P1

执行类型：E2E

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 标准商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入标准商品创建页。
3. 多规格商品 `TC_STD_DEFSPEC_083` 已设置默认规格为 `Large`。
4. 商品已加入菜单并下发至测试门店/渠道。

测试步骤：
1. 在终端打开该商品点单页。
2. 查看规格区域默认选中项。

预期结果：
1. 终端默认选中规格为 `Large`，与后台默认规格一致。

---

### 用例编号：TC-需求2-039

用例标题：多规格商品拖动调整规格顺序后保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_STD_DRAG_085`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品
- 就绪条件：页面出现商品列表表格

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_STD_DRAG_085` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入商品列表页。

测试步骤：
1. 商品名称查询框输入 `TC_STD_DRAG_085`，点击「查询」。
2. 记录当前规格展示顺序。
3. 拖动调整规格顺序（如将第 3 项拖到第 1 位）。
4. 点击「保存」后重新进入编辑页。

预期结果：
1. 保存成功，无报错拦截。
2. 编辑页规格顺序与拖动后顺序一致。

---


## 未匹配自动化源（未写入正文）

| 用例编号 | 用例标题 |
|------|------|
| TC-需求2-031 | 多规格商品选择默认规格后创建成功并展示默认规格价格 |
| TC-需求2-032 | 多规格商品未选择默认规格时列表展示最低规格价格 |

> 以上条目在商品/组自动化源文件中无同名块，且未配置标题别名；需先在源文件生成后再提取。
