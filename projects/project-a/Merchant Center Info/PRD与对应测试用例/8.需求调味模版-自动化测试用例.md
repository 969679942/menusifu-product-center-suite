# 8. 需求调味模版-自动化测试用例

对应需求：[8.需求调味模版.md](8.需求调味模版.md)

> 自动化块来源：`3.商品中心-商品管理-调味管理-自动化测试用例.md`；按**用例标题**匹配，不新增步骤。

---

# 商品中心-商品管理-调味管理 自动化测试用例

> 由 PRD 需求测试用例按标题从 `3.商品中心-商品管理-调味管理-自动化测试用例.md` 提取，不新增自动化步骤；规范见 [product-center-automation-test-case](.cursor/skills/product-center-automation-test-case/SKILL.md)。

## 调味管理自动化套件约定

> 写入 `3.商品中心-商品管理-调味管理-自动化测试用例.md` 顶部，**每条用例不再重复**。

## 登录与权限

- 使用执行计划绑定的测试账号登录商品中心（品牌视角）
- 门店/POS 用例另需门店上下文 S1 与 POS **30.16.6.7** 环境
- 账号须具备商品管理 / 调味管理读写权限

## 数据命名

| 类型 | 格式 | 示例 |
|------|------|------|
| 品牌调味组 | `TC_FLV_GRP_{正式编号}` | `TC_FLV_GRP_015` |
| 品牌调味项 | `TC_FLV_ITEM_{正式编号}` | `TC_FLV_ITEM_015` |
| 调味模板 | `TC_FLV_TPL_{正式编号}` | `TC_FLV_TPL_008` |
| 下发记录查询 | `TC_FLV_REC_{正式编号}` | `TC_FLV_REC_002` |
| 门店引用 | `TC_FLV_STORE_S1` / `TC_FLV_REF_{编号}` | `TC_FLV_REF_011` |
| PRD 需求前缀 | `D7_` / `D8_` / `D10_` + 场景 | `D8_模板下发-TC-需求8-020` |

- 执行前确认造数对象已存在（Readiness 未通过则 block/skip）

## 数据依赖用例（UI + 平台造数）

> 本文档 **仅含 UI/E2E 可执行用例**；接口造数/清理 **不写入本文档**。

- 前置含「已下发」「已存在模板/调味组/下发记录」→ 须写 **`数据标识`** + **Readiness**
- 步骤按固定名称/查询关键字定位，禁止「找一条满足条件的调味」

## 导航入口

| 子模块 | 路径 |
|--------|------|
| 品牌调味 | 商品管理 → 调味管理 → 调味 |
| 调味模板 | 商品管理 → 调味管理 → 调味模板 |
| 下发记录 | 商品管理 → 调味管理 → 下发记录 |
| 门店调味 | 门店商品管理 → 门店调味 |
| POS 全局调味 | POS 门店 S1 全局调味/点单入口（E2E） |

- 起始页与步骤第 1 步一致；保存/确认/查询统一加引号

## 跨模块 / POS（TC-FLV-XMOD-* / TC-FLV-POS-*）

- `TC-FLV-POS-*` 与部分 `TC-FLV-XMOD-*` 为 **E2E**
- 品牌下发、模版下发、菜单下发、同步 POS 由执行计划承担

## 自动化生成注意

- 正式前置里的 **「测试数据满足本场景（调味组/调味项/模板/下发记录等）」** 为占位句，**不触发** `数据标识` / Readiness
- **缺省页新建**（如 SEA-009）：前置应为「当前品牌下无调味数据」，步骤须字段级展开，禁止「点击新增入口，填写表单并确认保存」
- 命名：`TC_FLV_GRP_{正式编号}`、`TC_FLV_ITEM_{正式编号}`、`TC_FLV_TPL_{正式编号}`；PRD 小需求另用 `D7_/D8_/D10_` 前缀

---


## 下发记录

### 用例编号：TC-需求8-001

用例标题：调味模板列表页字段展示正确

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_001`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现调味模板列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_001` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 查看调味模板列表表头与各列字段。
2. 点击「调味数量」「适用门店」列查看明细（如有）。

预期结果：
1. 展示字段：模板名称、模板第二语言、模板说明、调味数量、适用门店。
2. 点击调味数量、适用门店展示对应信息。

---

### 用例编号：TC-需求8-002

用例标题：新增调味模板页面字段展示正确

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_002`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现调味模板列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_002` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 点击「新增」。
2. 查看新增模板表单各字段布局。

预期结果：
1. 展示字段：模板名称、模板第二语言、模板说明、调味信息（调味组名称、调味名称、调味组第二名称）。

---

### 用例编号：TC-需求8-003

用例标题：模板选择调味页按调味组+调味项展示正确

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现调味模板列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入调味模板页。

测试步骤：
1. 进入新增或编辑模板流程。
2. 打开「选择调味」页面，查看调味组与调味项列表及已选区。

预期结果：
1. 展示信息与调味组信息一致。

---

### 用例编号：TC-需求8-004

用例标题：模板已选调味展示已选数量与组名、调味名

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现调味模板列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入调味模板页。

测试步骤：
1. 进入新增或编辑模板流程。
2. 打开「选择调味」页面，查看调味组与调味项列表及已选区。

预期结果：
1. 已选调味展示：已选数量+调味组名称、调味名称、调味第二名称。

---

### 用例编号：TC-需求8-005

用例标题：模板下发选门店列表字段展示正确

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现调味模板列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入调味模板页。

测试步骤：
1. 选择门店/模板并执行下发。

预期结果：
1. 门店列表展示字段：门店名称、门店编码、区域、邮编、地址信息。
2. 已选门店展示：门店名称、门店编码。

---

### 用例编号：TC-需求8-006

用例标题：单门店品牌不展示调味模板入口

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_006`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现调味模板列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_006` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 以单门店品牌账号进入调味管理。
2. 查看导航是否展示「调味模板」菜单。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 单门店店铺不展示“调味模板”。

---

### 用例编号：TC-需求8-007

用例标题：按调味模板名称模糊或精确查询正确

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_007`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现调味模板列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_007` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 查询区输入与 `TC_FLV_TPL_007` 相关的关键字，点击「查询」。
2. 输入查询条件并执行查询。

预期结果：
1. 按调味模板名称模糊/精确查询正常。

---

### 用例编号：TC-需求8-008

用例标题：下发选门店页按门店名称或编码模糊查询正确

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现调味模板列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入调味模板页。

测试步骤：
1. 输入查询条件并执行查询。

预期结果：
1. 下发至选择门店页面支持按照门店名称和编码模糊查询。

---

### 用例编号：TC-需求8-009

用例标题：调味模板列表查询后重置恢复初始状态

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_009`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现调味模板列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_009` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 查询区输入与 `TC_FLV_TPL_009` 相关的关键字，点击「查询」。
2. 输入查询条件并执行查询。

预期结果：
1. 重置。

---

### 用例编号：TC-需求8-010

用例标题：新增调味模板名称为空时保存失败并高亮

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_010`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现「新增」或目标模板操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_010` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 点击「新增」。
2. 「模板名称」留空；选择至少一条调味。
3. 点击「确定」。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 新增调味模板，模版名称为空，点击确认提示必填项未填写，缺失的必填项高亮提示。

---

### 用例编号：TC-需求8-011

用例标题：模板名称支持中英文数字符号且不支持表情

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入调味模板页。

测试步骤：
1. 点击「新增」。
2. 「模板名称」输入 `TC_FLV_TPL_011_中文A1#`。
3. 选择至少一条调味后点击「确定」，确认合法字符可保存。
4. 再次「新增」，「模板名称」尝试输入表情符号，确认无法录入或保存失败。

预期结果：
1. 模板名称，支持中文、英文、数字、特殊符号，不支持表情。

---

### 用例编号：TC-需求8-012

用例标题：新增调味模板仅填必填项保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_012`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现「新增」或目标模板操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_012` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 点击「新增」。
2. 「模板名称」输入 `TC_FLV_TPL_012`；选择至少一条调味。
3. 「模板说明」等非必填留空。
4. 点击「确定」。

预期结果：
1. 新增调味模板，只填必填参数，能新增成功。

---

### 用例编号：TC-需求8-013

用例标题：新增调味模板填写全部字段保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_013`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现「新增」或目标模板操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_013` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 确认当前为无调味缺省页（可见行业调味库说明或「新增调味」入口）。
2. 点击「新增调味」。
3. 确认页面跳转至【新增调味】表单页。
4. 「调味组名称」输入 `TC_FLV_GRP_013`。
5. 「调味名称」输入 `TC_FLV_ITEM_013`。
6. 「默认价格」输入 `10.00`。
7. 点击「确定」。
8. 返回列表，按 `TC_FLV_GRP_013` 查询或浏览列表，确认新建调味组及调味项存在。

预期结果：
1. 新增调味模板，所有字段都添加了，能新增成功。

---

### 用例编号：TC-需求8-014

用例标题：新增调味模板不选调味信息也可保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_014`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现「新增」或目标模板操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_014` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 确认当前为无调味缺省页（可见行业调味库说明或「新增调味」入口）。
2. 点击「新增调味」。
3. 确认页面跳转至【新增调味】表单页。
4. 「调味组名称」输入 `TC_FLV_GRP_014`。
5. 「调味名称」输入 `TC_FLV_ITEM_014`。
6. 「默认价格」输入 `10.00`。
7. 点击「确定」。
8. 返回列表，按 `TC_FLV_GRP_014` 查询或浏览列表，确认新建调味组及调味项存在。

预期结果：
1. 新增调味模板，不选择调味信息，能新增成功。

---

### 用例编号：TC-需求8-015

用例标题：调味模板名称与已有模板重复时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_015`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_015` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 确认已存在模板 `TC_FLV_TPL_015_DUP`（执行计划造数）。
2. 点击「新增」。
3. 「模板名称」输入与 `TC_FLV_TPL_015_DUP` 相同的名称；选择至少一条调味。
4. 点击「确定」。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 模板名称不可重复。

---

### 用例编号：TC-需求8-016

用例标题：调味模板名称重复时提示内容重复

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_016`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_016` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 确认已存在模板 `TC_FLV_TPL_016_DUP`（执行计划造数）。
2. 点击「新增」。
3. 「模板名称」输入与 `TC_FLV_TPL_016_DUP` 相同的名称；选择至少一条调味。
4. 点击「确定」。

预期结果：
1. 页面仍停留在当前页或弹窗，未出现不应出现的成功提示。
2. 有重复数据，点击保存时，提示内容重复。

---

### 用例编号：TC-需求8-017

用例标题：编辑调味模板信息保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_017`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现「新增」或目标模板操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_017` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 打开模板 `TC_FLV_TPL_017` 编辑页。
2. 修改模板信息或已选调味后保存。

预期结果：
1. 编辑调味模板信息正常。

---

### 用例编号：TC-需求8-018

用例标题：调味模板下新增调味保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_018`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现「新增」或目标模板操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_018` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 确认当前为无调味缺省页（可见行业调味库说明或「新增调味」入口）。
2. 点击「新增调味」。
3. 确认页面跳转至【新增调味】表单页。
4. 「调味组名称」输入 `TC_FLV_GRP_018`。
5. 「调味名称」输入 `TC_FLV_ITEM_018`。
6. 「默认价格」输入 `10.00`。
7. 点击「确定」。
8. 返回列表，按 `TC_FLV_GRP_018` 查询或浏览列表，确认新建调味组及调味项存在。

预期结果：
1. 调味模板下新增调味正常。

---

### 用例编号：TC-需求8-019

用例标题：调味模板下删除调味成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_019`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_019` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 选择模板 `TC_FLV_TPL_019`。
2. 点击「删除」并在二次确认中确认。

预期结果：
1. 调味模板下调味删除正常。

---

### 用例编号：TC-需求8-020

用例标题：删除调味模板成功（含模板内调味）

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_020`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_020` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 选择模板 `TC_FLV_TPL_020`。
2. 点击「删除」并在二次确认中确认。

预期结果：
1. 调味模板下有调味信息，可直接删除。

---

### 用例编号：TC-需求8-021

用例标题：删除调味模板或模板内调味时二次确认

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_021`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_021` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 选择模板 `TC_FLV_TPL_021`。
2. 点击「删除」并在二次确认中确认。

预期结果：
1. 调味模板息删除时有二次确认按钮。

---

### 用例编号：TC-需求8-022

用例标题：调味模板下发后门店全局调味与模板一致

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_022`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现「新增」或目标模板操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_022` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 选择门店/模板并执行下发。

预期结果：
1. 可选择模板进行下发，下发至对应门店后，门店中全局调味信息与模板中一致。

---

### 用例编号：TC-需求8-023

用例标题：调味模板再次下发覆盖门店已有调味

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_023`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现「新增」或目标模板操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_023` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 选择门店/模板并执行下发。

预期结果：
1. 模板 A 下发至门店 S1 后，门店调味与模板 A 一致。
2. 模板 B 再下发至门店 S1 后，门店 S1 展示模板 B 的调味（覆盖模板 A 在 S1 的数据）。

---

### 用例编号：TC-需求8-024

用例标题：编辑调味模板未再次下发前门店调味不变再次下发后覆盖

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_024`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面出现「新增」或目标模板操作入口

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_024` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 在品牌侧编辑模板 T1（如修改某调味项价格或增删模板内调味），保存。
2. 进入 门店商品管理 → 门店调味（S1），核对调味列表，**不执行**再次下发。
3. 将模板 T1 再次下发至门店 S1。
4. 再次查看 S1 门店调味列表。

预期结果：
1. 步骤 2：门店调味仍与步骤 0 快照一致（编辑模板未改变已下发门店数据）。
2. 步骤 4：门店调味与编辑后的模板 T1 一致（再次下发后覆盖）。

---

### 用例编号：TC-需求8-025

用例标题：模板说明达250字符边界保存成功

优先级：P2

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_TPL_025`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 调味模板
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_TPL_025` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入调味模板页。

测试步骤：
1. 新增调味模板，模板名称等必填合法，模板说明填写恰好 250 字符。
2. 保存并查看模板详情/列表。

预期结果：
1. 保存成功。
2. 模板说明完整保存为 250 字符（或按现网展示规则完整可见）。

---

### 用例编号：TC-需求8-026

用例标题：调味下发记录列表字段展示正确

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_REC_001`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 下发记录
- 就绪条件：页面出现下发记录列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_REC_001` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入下发记录页。

测试步骤：
1. 选择门店/模板并执行下发。

预期结果：
1. 展示字段：任务名称、下发门店数、执行状态、操作人、执行时间。

---

### 用例编号：TC-需求8-027

用例标题：按任务名称模糊或精确查询下发记录正确

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_REC_002`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 下发记录
- 就绪条件：页面出现下发记录列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_REC_002` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入下发记录页。

测试步骤：
1. 查询区输入与 `TC_FLV_REC_002` 相关的关键字，点击「查询」。
2. 输入查询条件并执行查询。

预期结果：
1. 支持按任务名称模糊/精确查询。

---

### 用例编号：TC-需求8-028

用例标题：按门店或执行状态查询下发记录正确

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_REC_003`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 下发记录
- 就绪条件：页面出现下发记录列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_REC_003` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入下发记录页。

测试步骤：
1. 查询区输入与 `TC_FLV_REC_003` 相关的关键字，点击「查询」。
2. 输入查询条件并执行查询。

预期结果：
1. 支持按门店/执行状态查询。

---

### 用例编号：TC-需求8-029

用例标题：下发记录组合查询条件生效正确

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_REC_004`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 下发记录
- 就绪条件：页面出现下发记录列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_REC_004` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入下发记录页。

测试步骤：
1. 查询区输入与 `TC_FLV_REC_004` 相关的关键字，点击「查询」。
2. 输入查询条件并执行查询。

预期结果：
1. 组合查询。

---

### 用例编号：TC-需求8-030

用例标题：下发记录查询后重置恢复初始列表

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_REC_005`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 下发记录
- 就绪条件：页面出现下发记录列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_REC_005` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入下发记录页。

测试步骤：
1. 查询区输入与 `TC_FLV_REC_005` 相关的关键字，点击「查询」。
2. 输入查询条件并执行查询。

预期结果：
1. 重置。

---

### 用例编号：TC-需求8-031

用例标题：点击下发门店数可查看本次下发门店明细

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_REC_006`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 下发记录
- 就绪条件：页面出现下发记录列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_REC_006` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入下发记录页。

测试步骤：
1. 打开 商品管理 → 调味管理 → 下发记录。
2. 找到目标记录，点击「下发门店数」列数值（或等效入口）。
3. 查看弹窗/详情页中的门店列表。

预期结果：
1. 可打开门店明细，展示本次下发涉及的门店。
2. 明细含门店名称、门店编码（与 §12 已选门店展示字段一致）。

---

### 用例编号：TC-需求8-032

用例标题：调味下发任务名称符合模板名加11位数字规则

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_FLV_REC_007`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 调味管理 → 下发记录
- 就绪条件：页面出现下发记录列表或查询区

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_FLV_REC_007` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入下发记录页。

测试步骤：
1. 将模板 T1 下发至至少一家门店。
2. 打开 下发记录，找到刚产生的记录。
3. 核对任务名称字段。

预期结果：
1. 任务名称以模板名称「华东模板」为前缀。
2. 后缀为 **11 位数字**（与现网生成规则一致，如 `华东模板12345678901`）。

---

