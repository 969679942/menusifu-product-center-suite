# 5. 需求套餐组-自动化测试用例

对应需求：[5.需求套餐组.md](5.需求套餐组.md)

| 章节 | 条数 |
|------|------|
| 套餐组管理 | 34 |
| S07 套餐商品规则 | 22 |
| S08 商品内套餐组合搭配 | 4 |
| S07 补充 | 2 |
| S08 补充 | 2 |

> 自动化块来源：`1.商品中心-商品管理-商品-自动化测试用例.md`、`2.商品中心-商品管理-组-自动化测试用例.md`；按**用例标题**匹配，不新增步骤。

---

# 商品中心-商品管理-组 自动化测试用例

> 由 PRD 需求测试用例按标题从 `2.商品中心-商品管理-组-自动化测试用例.md` 提取，不新增自动化步骤；规范见 [product-center-automation-test-case](.cursor/skills/product-center-automation-test-case/SKILL.md)。

## 组管理自动化套件约定

> 写入 `*-组-自动化测试用例.md` 顶部，**每条用例不再重复**。

## 登录与权限

- 使用执行计划绑定的测试账号登录商品中心
- 账号须具备组管理（规格/口味/做法/加料/套餐组）读写权限

## 数据命名

| 类型 | 格式 | 示例 |
|------|------|------|
| 新建组名 | `组名称-autocreate-{类型小写}-{正式编号}{场景后缀}` | `组名称-autocreate-spec-005b` |
| 新建明细名 | `{明细前缀}-autocreate-{类型}-{编号}` | `规格明细-autocreate-spec-004-01` |
| 数据依赖-单对象 | `TC_GRP_{类型}_{编号}` | `TC_GRP_SPEC_013` |
| 数据依赖-引用 | `{类型}_REF_{编号}` / 商品 `STD_REF_{编号}` | `TC_SPEC_GRP_REF_013` |
| 重复名校验 | 沿用正式用例反引号名 | `GRP_DUP`、`TASTE_DUP` |

- 执行前确认当前品牌下不存在将创建的同名的组/明细（或由平台 teardown 清理）

## 数据依赖用例（UI + 平台造数）

> 本文档 **仅含 UI 可执行用例**。接口造数/清理 **不写入本文档**，由执行计划完成。

- UI 用例写 **`数据标识`**、**`数据准备`**（一行指向执行计划）、**Readiness**、**`失败分类`**
- 步骤按固定组名/明细名/商品名查询定位，禁止「存在规格组 A」类模糊前置

## 导航入口

| 组类型 | 列表 | 新增 |
|--------|------|------|
| 规格组 | 商品管理 → 规格组 | 列表 → 新增 |
| 口味组 | 商品管理 → 口味组 | 列表 → 新增 |
| 做法组 | 商品管理 → 做法组 | 列表 → 新增 |
| 加料组 | 商品管理 → 加料组 | 列表 → 新增 |
| 套餐组 | 商品管理 → 套餐组 | 列表 → 新增（固定/组合搭配按用例） |

- 保存/确认按钮：组编辑页统一写「保存」或「确认」（与现网一致，以用例步骤为准）

## 步骤增强

- **禁止**「组名称或明细名称留空」「仅填组名称」「按需填写」等模糊步骤
- 必填项缺失：拆 `-A` 组名称为空 / `-B` 明细名称为空（单步骤仅测一项）
- 列表/查询：有已知关键字时用反引号具名
- 被引用/删除/同步类：写清 `数据标识` + Readiness

---


## 套餐组管理

### 用例编号：TC-需求5-001

用例标题：固定搭配与组合搭配列表页展示正确

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组列表页。

测试步骤：
1. 查看固定搭配、组合搭配页签及列表区、查询区与新增入口。

预期结果：
1. 固定搭配、组合搭配页签均可见。
2. 各页签下可见列表区、查询区与新增入口。

---

### 用例编号：TC-需求5-002

用例标题：套餐组查询与重置正确

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组列表页。

测试步骤：
1. 按名称查询套餐组后重置。

预期结果：
1. 重置后展示全部套餐组。

---

### 用例编号：TC-需求5-003

用例标题：新增组合搭配套餐组无商品保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组 → 新增组合搭配
- 就绪条件：页面出现组名称或套餐组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 新增加料组类型为组合搭配，不添加任何套餐商品，保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，提示须添加套餐商品/子项。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求5-004

用例标题：新增固定搭配仅填必填项保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组 → 新增固定搭配
- 就绪条件：页面出现组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 「组名称」输入 `组名称-autocreate-pkg-004`。
2. 点击「添加套餐商品」。
3. 「套餐商品名称」输入 `明细-autocreate-pkg-004-01`。
4. 选择执行计划种子数据中的 1 个有效套餐商品。
5. 点击「确认」保存明细。
6. 点击「保存」。
7. 返回列表，组名称查询框输入 `组名称-autocreate-pkg-004`，点击「查询」。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求5-005

用例标题：新增组合搭配仅填必填项保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组 → 新增组合搭配
- 就绪条件：页面出现组名称或套餐组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 「组名称」输入 `组名称-autocreate-pkg-005`。
2. 点击「添加套餐商品」。
3. 「套餐商品名称」输入 `明细-autocreate-pkg-005-01`。
4. 选择执行计划种子数据中的 1 个有效套餐商品。
5. 点击「确认」保存明细。
6. 点击「保存」。
7. 返回列表，组名称查询框输入 `组名称-autocreate-pkg-005`，点击「查询」。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求5-006

用例标题：组合搭配套餐组名称品牌内不可重复

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`COMBO_A`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `COMBO_A` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入套餐组新增页。

测试步骤：
1. 「组名称」输入 `COMBO_A`。
2. 点击「添加套餐商品」，「套餐商品名称」输入 `明细-autocreate-pkg-006-01`。
3. 选择执行计划种子数据中的 1 个有效套餐商品。
4. 点击「确认」保存明细。
5. 点击「保存」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，提示组名称不可重复。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求5-007

用例标题：固定搭配与组合搭配组名允许重复

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`SET_MEAL_1`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `SET_MEAL_1` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入套餐组新增页。

测试步骤：
1. 组名称查询框输入 `SET_MEAL_1`，点击「查询」。
2. 新增组合搭配组名同为 `SET_MEAL_1`。

预期结果：
1. 保存成功（不同类型间组名可重复）。

---

### 用例编号：TC-需求5-008

用例标题：套餐组份数规则校验同加料组

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 分别验证：最少>最多失败；最少=最多=0 失败；最少0最多2 成功。

预期结果：
1. 与 TC-GRP-ADD-007~009 同类规则一致。

---

### 用例编号：TC-需求5-009

用例标题：已被引用套餐组新增商品不自动同步到商品

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_PKG_009`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 套餐组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_PKG_009` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入套餐组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_PKG_009`，点击「查询」。
2. 向 P 新增套餐商品 3 并保存，查看引用套餐商品。

预期结果：
1. 套餐组含商品 3；引用该组的套餐商品不自动增加商品 3（或有提示）。

---

### 用例编号：TC-需求5-010

用例标题：移除套餐商品后仍满足规则时引用商品同步更新

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_PKG_010`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_PKG_010` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入套餐组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_PKG_010`，点击「查询」。
2. 从套餐组 A 移除商品 3 并保存，查看套餐商品 X。

预期结果：
1. 套餐组 A 无商品 3；套餐商品 X 引用内容同步移除商品 3。

---

### 用例编号：TC-需求5-011

用例标题：移除套餐商品后不满足规则时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 删除该商品并保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，提示不满足套餐规则；组内商品结构不变。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求5-012

用例标题：修改规则后移除商品可保存且同步商品

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 移除商品 3，同时将规则改为最少 1、最多 1，保存。

预期结果：
1. 保存成功；引用商品同步更新。

---

### 用例编号：TC-需求5-013

用例标题：编辑套餐组基础信息后引用商品同步

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_PKG_013`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面出现组列表表格

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_PKG_013` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入套餐组编辑页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_PKG_013`，点击「查询」，进入编辑页。
2. 修改套餐组名称/编码，保存后查看 P、Q。

预期结果：
1. 商品 P、Q 中套餐组信息与编辑后一致。

---

### 用例编号：TC-需求5-014

用例标题：编辑套餐组下商品加价后引用商品同步

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面出现组列表表格

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组编辑页。

测试步骤：
1. 修改组内某商品单次加价/默认可加购等（非 BR 除外字段按产品定义），保存。

预期结果：
1. 引用该套餐组的商品中对应子项信息与编辑后一致（价格/默认选中除外见 BR-GRP-033）。

---

### 用例编号：TC-需求5-015

用例标题：编辑不满足套餐规则时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面出现组列表表格

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组编辑页。

测试步骤：
1. 将商品 3 改为默认选中，保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，提示不满足套餐规则。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求5-016

用例标题：套餐组下有商品时不可直接删除组

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 对仍含套餐商品的套餐组点击「删除」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提示禁止删除或须先移除组内商品。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求5-017

用例标题：清空组内商品后套餐组可删除成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 移除组内全部商品后删除套餐组。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求5-018

用例标题：新增套餐组选择商品页点击「取消」不保存

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 编辑套餐组，添加商品弹窗选择商品后点击「取消」。

预期结果：
1. 套餐组仍只有商品 A。

---

### 用例编号：TC-需求5-019

用例标题：组合搭配支持添加多规格商品保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 在组合搭配中添加含 S/M/L 的多规格商品，保存。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求5-020

用例标题：新增固定搭配填写全部字段保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组 → 新增固定搭配
- 就绪条件：页面出现组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 「组名称」输入 `组名称-autocreate-pkg-021`。
2. 点击「添加套餐商品」。
3. 「套餐商品名称」输入 `明细-autocreate-pkg-021-01`。
4. 选择执行计划种子数据中的 1 个有效套餐商品。
5. 点击「确认」保存明细。
6. 点击「保存」。
7. 返回列表，组名称查询框输入 `组名称-autocreate-pkg-021`，点击「查询」。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表可搜到该套餐组且字段与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求5-021

用例标题：新增组合搭配套餐组必填项缺失保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组 → 新增组合搭配
- 就绪条件：页面出现组名称或套餐组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 确认「组名称」输入框为空，不输入任何内容。
2. 点击「保存」或「确认」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 「组名称」字段出现必填校验提示。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求5-022

用例标题：套餐份数最少和最多相同时保存成功

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 新增组合搭配，最少与最多均填 2，添加套餐商品，保存。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 列表/详情与提交一致。
3. 关键字段与步骤输入一致。

---

### 用例编号：TC-需求5-023

用例标题：移除默认选中套餐商品后仍满足规则可保存

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_PKG_024`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_PKG_024` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入套餐组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_PKG_024`，点击「查询」。
2. 从套餐组 A 移除商品 3 并保存。
3. 查看套餐商品 X。

预期结果：
1. 保存成功，无报错拦截。
2. 套餐商品 X 引用内容同步移除商品 3。

---

### 用例编号：TC-需求5-024

用例标题：新增套餐商品后不满足套餐规则保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组 → 新增
- 就绪条件：页面出现「组名称」或组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 向套餐组新增商品 4 并设为默认选中，保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，提示不满足套餐规则。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求5-025

用例标题：套餐商品选择页交互验证正确

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_GRP_PKG_026`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `TC_GRP_PKG_026` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入套餐组新增页。

测试步骤：
1. 组名称查询框输入 `TC_GRP_PKG_026`，点击「查询」。
2. 编辑套餐组，打开套餐商品选择弹窗。
3. 搜索、勾选商品后确认。

预期结果：
1. 弹窗支持搜索与多选。
2. 确认后组内含所选套餐商品。

---

### 用例编号：TC-需求5-026

用例标题：新增固定搭配套餐组无商品保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组 → 新增固定搭配
- 就绪条件：页面出现组名称输入框

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 填写组名称，不添加任何套餐商品，保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，提示须添加套餐商品/子项。
3. 未产生不符合预期的数据变更。

---


## 补充用例（组模块缺口 · v3.3）

### 用例编号：TC-需求5-027

用例标题：组合搭配套餐组名称仅大小写不同视为重复

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`ComboGroup`
- extra_0：`combogroup`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. Readiness：列表按 `ComboGroup` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入套餐组新增页。

测试步骤：
1. 「组名称」输入 `combogroup`。
2. 点击「添加套餐商品」，「套餐商品名称」输入 `明细-autocreate-pkg-028-01`。
3. 选择执行计划种子数据中的 1 个有效套餐商品。
4. 点击「确认」保存明细。
5. 点击「保存」。

预期结果：
1. 提交失败，页面提示包含 `SYSTEM-0002:参数冲突`。

---


## 补充用例（组模块缺口 · v3.4）

### 用例编号：TC-需求5-028

用例标题：套餐组名称超长保存后按100字符规则处理

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 套餐组名称输入超过 100 字符且包含首尾空格、连续空格和特殊字符的内容。
2. 补全其他必填项并保存。
3. 查看已保存的套餐组名称。

预期结果：
1. 出现保存成功提示，无报错弹窗。
2. 最终保存的套餐组名称为前 100 个有效字符，首尾空格已去除。

---

### 用例编号：TC-需求5-029

用例标题：套餐组最少选择大于最多选择时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 最少选择填写 `3`。
2. 最多选择填写 `1`。
3. 补全其他必填项并保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，页面提示最少选择需小于等于最多选择。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求5-030

用例标题：套餐组最少选择和最多选择同时为0时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 最少选择填写 `0`。
2. 最多选择填写 `0`。
3. 补全其他必填项并保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求5-031

用例标题：套餐组子项默认数量超过最多选择时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 最多选择填写 `2`。
2. 新增 1 个套餐子项，默认数量填写 `3`。
3. 补全其他必填项并保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求5-032

用例标题：套餐组子项可加购数量不足最少选择时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 最少选择填写 `2`。
2. 新增 1 个套餐子项，可加购数量填写 `1`。
3. 补全其他必填项并保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求5-033

用例标题：套餐组相同商品合并开启时C端合并展示

优先级：P1

执行类型：E2E

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 在终端打开引用该套餐组的套餐选择页。
2. 查看 `PKG_ITEM_034` 的展示形式。

预期结果：
1. 终端对同一商品合并展示；点击后再选择具体规格。

---

### 用例编号：TC-需求5-034

用例标题：套餐组相同商品合并关闭时C端分开展示

优先级：P1

执行类型：E2E

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面出现组列表或查询区

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 在终端打开引用该套餐组的套餐选择页。
2. 查看 `PKG_ITEM_035` 的展示形式。

预期结果：
1. 终端按不同规格分开展示，不合并为一条。

---


## S07 套餐商品规则

### 用例编号：TC-需求5-035

用例标题：套餐商品可选择已有固定搭配套餐组

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。
3. 系统中已创建固定搭配类型的套餐组。

测试步骤：
1. 点击选择已有套餐组中的「固定搭配」。
2. 查看套餐组选择列表。
3. 选择一个套餐组，加入右侧已选区域。
4. 点击确认返回创建页面。

预期结果：
1. 默认加载所有已创建的固定搭配套餐组。
2. 选中的套餐组加入右侧已选区域，且支持从已选区域移除。
3. 未选择套餐组时确认按钮不可点击，选择后确认按钮高亮可点击。

---

### 用例编号：TC-需求5-036

用例标题：套餐商品可按名称搜索固定搭配套餐组

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。
3. 系统中已创建多个固定搭配套餐组。

测试步骤：
1. 输入套餐组名称关键字进行搜索。
2. 查看搜索结果。
3. 清空搜索条件。
4. 再次查看默认列表。

预期结果：
1. 支持按套餐组名称进行模糊搜索。
2. 清空搜索条件后恢复默认列表展示。

---

### 用例编号：TC-需求5-037

用例标题：套餐商品可选择已有组合搭配套餐组

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。
3. 系统中已创建组合搭配类型的套餐组。

测试步骤：
1. 点击选择已有套餐组中的「组合搭配」。
2. 查看套餐组选择列表。
3. 选择一个套餐组，加入右侧已选区域。
4. 点击确认返回创建页面。

预期结果：
1. 默认加载所有已创建的组合搭配套餐组。
2. 选中的套餐组加入右侧已选区域，且支持从已选区域移除。
3. 未选择套餐组时确认按钮不可点击，选择后确认按钮高亮可点击。

---

### 用例编号：TC-需求5-038

用例标题：套餐商品可新增固定搭配套餐组

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_SET_MEAL_A`
- extra_0：`TC_SET_MEAL_B`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_SET_MEAL_A` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 当前品牌下不存在测试商品 `TC_PKG_006`。
4. 从起始页面进入套餐商品创建页。


测试步骤：
1. 「商品名称」输入 `TC_PKG_006`。
2. 展开「高级设置」，「起售数量」输入 `1`（或保持默认 `1`）。
3. 不选择商品分类。
4. 「标准价」输入 `10.00`。
5. 点击「新增固定搭配」。
6. 输入组名 `TC_FIXED_GROUP_001`，选择商品 `TC_SET_MEAL_A` 和 `TC_SET_MEAL_B`。
7. 点击「保存」。
8. 返回列表，商品名称查询框输入 `TC_PKG_006`，点击「查询」，进入编辑页，查看套餐组信息。

预期结果：
1. 套餐商品保存成功。
2. 商品内新增固定搭配组 `TC_FIXED_GROUP_001`，并包含 `TC_SET_MEAL_A` 和 `TC_SET_MEAL_B`。

---

### 用例编号：TC-需求5-039

用例标题：套餐商品可新增组合搭配套餐组

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_COMBO_A`
- extra_0：`TC_COMBO_B`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_COMBO_A` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 当前品牌下不存在测试商品 `TC_PKG_007`。
4. 从起始页面进入套餐商品创建页。


测试步骤：
1. 「商品名称」输入 `TC_PKG_007`。
2. 展开「高级设置」，「起售数量」输入 `1`（或保持默认 `1`）。
3. 不选择商品分类。
4. 「标准价」输入 `10.00`。
5. 点击「新增组合搭配」。
6. 输入组名 `TC_COMBO_GROUP_001`，设置最少选择份数 `1`、最多选择份数 `2`、份数内免费 `1`。
7. 选择商品 `TC_COMBO_A` 和 `TC_COMBO_B`。
8. 点击「保存」。
9. 返回列表，商品名称查询框输入 `TC_PKG_007`，点击「查询」，进入编辑页，查看套餐组信息。

预期结果：
1. 套餐商品保存成功。
2. 商品内新增组合搭配组 `TC_COMBO_GROUP_001`，组规则为最少选择份数 `1`、最多选择份数 `2`、份数内免费 `1`。
3. 组合搭配组中展示 `TC_COMBO_A` 和 `TC_COMBO_B` 两个子项。

---

### 用例编号：TC-需求5-040

用例标题：未选择套餐组时确认按钮不可点击

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。

测试步骤：
1. 「商品名称」输入 `商品名称-autocreate-040c`。
2. 展开「高级设置」，「起售数量」输入 `1`。
3. 「标准价」输入 `10.00`。
4. 套餐组：不选择任何套餐组。
5. 点击「保存」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。

---

### 用例编号：TC-需求5-041

用例标题：选择套餐组后确认按钮可点击并返回创建页

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。

测试步骤：
1. 选择 1 个有效套餐组。
2. 点击确认。

预期结果：
1. 确认按钮变为可点击状态。
2. 点击确认后返回套餐商品创建页，并带回已选套餐组。

---

### 用例编号：TC-需求5-042

用例标题：已选固定搭配套餐组可从右侧移除

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。

测试步骤：
1. 在右侧已选区域点击移除。
2. 查看右侧已选区域。

预期结果：
1. 所选固定搭配套餐组被成功移除。
2. 右侧已选区域与当前选择结果一致。

---

### 用例编号：TC-需求5-043

用例标题：已选组合搭配套餐组可从右侧移除

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。

测试步骤：
1. 在右侧已选区域点击移除。
2. 查看右侧已选区域。

预期结果：
1. 所选组合搭配套餐组被成功移除。
2. 右侧已选区域与当前选择结果一致。

---

### 用例编号：TC-需求5-044

用例标题：组合搭配套餐组按名称模糊搜索成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_DATA_PKG_044`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_DATA_PKG_044` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入套餐商品创建页。

测试步骤：
1. 输入套餐组名称关键字。
2. 点击查询。

预期结果：
1. 列表按关键字返回匹配的组合搭配套餐组。
2. 不匹配的套餐组不展示。

---

### 用例编号：TC-需求5-045

用例标题：组合搭配套餐组清空搜索条件后恢复默认列表

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。

测试步骤：
1. 清空搜索条件。
2. 查看套餐组列表。

预期结果：
1. 列表恢复默认加载状态。
2. 默认列表展示所有可选组合搭配套餐组。

---

### 用例编号：TC-需求5-046

用例标题：未选择任何套餐组时套餐商品不可提交

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。
3. 当前品牌下不存在测试商品 `TC_PKG_046`。

测试步骤：
1. 商品名称填写 `TC_PKG_046`。
2. 起售数量填写 `1`。
3. 标准价填写 `10.00`。
4. 不选择任何套餐组。
5. 点击「保存」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 页面提示需先选择或新增套餐组。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求5-047

用例标题：套餐商品同时引用固定搭配与组合搭配套餐组成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。
3. 当前品牌下不存在测试商品 `TC_PKG_049`。

测试步骤：
1. 商品名称填写 `TC_PKG_049`。
2. 起售数量填写 `1`。
3. 标准价填写 `10.00`。
4. 选择 1 个固定搭配套餐组。
5. 选择 1 个组合搭配套餐组。
6. 点击「保存」。

预期结果：
1. 保存成功，无报错拦截。
2. 商品详情中同时展示固定搭配与组合搭配套餐组。

---

### 用例编号：TC-需求5-048

用例标题：套餐组下商品全部删除后该套餐项在商品中不展示

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。
3. 套餐商品已引用 1 个套餐组，且该套餐组下当前存在多个子项商品。

测试步骤：
1. 在商品内将该套餐组下全部子项移除。
2. 点击「保存」。
3. 重新进入商品编辑页查看套餐组展示。

预期结果：
1. 保存成功，无报错拦截。
2. 该空套餐项不再在商品中展示。

---

### 用例编号：TC-需求5-049

用例标题：组合搭配套餐组按名称精确搜索成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`COMBO_GRP_EXACT`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `COMBO_GRP_EXACT` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入套餐商品创建页。

测试步骤：
1. 输入完整套餐组名称 `COMBO_GRP_EXACT`。
2. 点击查询。

预期结果：
1. 列表返回名称精确匹配的套餐组。
2. 无关套餐组不展示。

---

### 用例编号：TC-需求5-050

用例标题：组合搭配最少选择份数、最多选择份数、份数内免费为必填

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。

测试步骤：
1. 点击「新增组合搭配」（进入组合搭配配置弹窗）。
2. 保持最少选择份数、最多选择份数、份数内免费为空。
3. 点击「确认」。

预期结果：
1. 提交失败，页面拦截保存。
2. 三个规则字段出现必填提示。

---

### 用例编号：TC-需求5-051

用例标题：组合搭配规则输入不合法时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。

测试步骤：
1. 点击「新增组合搭配」（进入组合搭配配置弹窗）。
2. 最少选择份数填写 `2`。
3. 最多选择份数填写 `1`。
4. 份数内免费填写 `3`。
5. 点击「确认」。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 规则字段出现最少≤最多、免费份数需位于区间内的校验提示。
3. 未产生不符合预期的数据变更。

---

### 用例编号：TC-需求5-052

用例标题：移除已添加套餐组后组规则校验生效

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。
3. 套餐商品已添加组合搭配套餐组。

测试步骤：
1. 移除组合搭配套餐组中的部分子项。
2. 尝试保存当前商品。

预期结果：
1. 当移除结果不满足最少选择份数等规则时，页面拦截保存。
2. 页面提示当前套餐组规则不满足保存条件。

---

### 用例编号：TC-需求5-053

用例标题：套餐商品引用做法组整组后保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_PKG_063`
- extra_0：`COOK_GRP_001`
- extra_1：`PKG_GRP_001`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_PKG_063` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入套餐商品创建页。

测试步骤：
1. 商品名称填写 `TC_PKG_063`。
2. 起售数量填写 `1`；标准价填写 `10.00`。
3. 选择套餐组 `PKG_GRP_001`。
4. 在商品属性区域引用做法组 `COOK_GRP_001`。
5. 点击「保存」。
6. 返回列表，商品名称查询框输入 `TC_PKG_063`，点击「查询」，进入详情页。

预期结果：
1. 保存成功，无报错拦截。
2. 详情中展示已整组引用的做法组 `COOK_GRP_001`。

---

### 用例编号：TC-需求5-054

用例标题：套餐商品引用加料组整组后保存成功

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_PKG_064`
- extra_0：`ADDON_GRP_001`
- extra_1：`PKG_GRP_001`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_PKG_064` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入套餐商品创建页。

测试步骤：
1. 商品名称填写 `TC_PKG_064`。
2. 起售数量填写 `1`；标准价填写 `10.00`。
3. 选择套餐组 `PKG_GRP_001`。
4. 在商品属性区域引用加料组 `ADDON_GRP_001`。
5. 点击「保存」。
6. 返回列表，商品名称查询框输入 `TC_PKG_064`，点击「查询」，进入详情页。

预期结果：
1. 保存成功，无报错拦截。
2. 详情中展示已整组引用的加料组 `ADDON_GRP_001`。

---

### 用例编号：TC-需求5-055

用例标题：套餐商品内不可单独添加组子项仅可移除已引用子项

优先级：P1

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。
3. 套餐商品已引用 1 个口味组、做法组或加料组。

测试步骤：
1. 查看已引用组选项区域。
2. 尝试直接新增组内单个子项。
3. 移除 1 个已引用子项。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。

---

### 用例编号：TC-需求5-056

用例标题：套餐必选子项停用后终端不可正常点单

优先级：P1

执行类型：E2E

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 商品 → 新增商品 → 套餐商品
- 就绪条件：页面出现「商品名称」输入框

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. 从起始页面进入套餐商品创建页。
3. 套餐商品 `TC_PKG_STOP_070` 已启用，且套餐组内含必选子项商品 `SUB_A`。
4. `SUB_A` 在商品库中为启用状态，套餐已下发至测试门店。
5. 终端可正常点选该套餐。

测试步骤：
1. 在商品库将 `SUB_A` 停用并下发至门店。
2. 在终端再次尝试点选套餐 `TC_PKG_STOP_070`。

预期结果：
1. 终端无法将套餐 `TC_PKG_STOP_070` 成功加购。
2. 终端出现子项 `SUB_A` 不可用相关提示。

---


## S08 商品内套餐组合搭配

### 用例编号：TC-需求5-057

用例标题：套餐商品内编辑口味组加价和默认选中仅对当前商品生效

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_PKG_FLAVOR_001`
- extra_0：`TC_PKG_EDIT_ATTR_069`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品
- 就绪条件：页面出现商品列表表格

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_PKG_FLAVOR_001` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入商品列表页。

测试步骤：
1. 打开 `TC_PKG_EDIT_ATTR_069` 编辑页。
2. 在商品属性中将口味 B 设为默认选中，商品内加价改为 `2.00`。
3. 点击「保存」。
4. 返回列表，商品名称查询框输入 `TC_PKG_FLAVOR_001`，点击「查询」，进入编辑页。
5. 进入口味组管理查看 `TC_PKG_FLAVOR_001` 原始配置。

预期结果：
1. 保存成功，无报错拦截。
2. 套餐商品内展示口味 B 为默认选中、加价为 `2.00`。
3. 口味组管理中原始默认选中与加价不因本次编辑而改变。

---

### 用例编号：TC-需求5-058

用例标题：套餐商品内编辑做法组加价和默认选中仅对当前商品生效

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_PKG_METHOD_071`
- extra_0：`TC_PKG_METHOD_EDIT_071`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品
- 就绪条件：页面出现商品列表表格

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_PKG_METHOD_071` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入商品列表页。

测试步骤：
1. 打开 `TC_PKG_METHOD_EDIT_071` 编辑页。
2. 在商品属性中将做法 B 设为默认选中，商品内加价改为 `2.00`。
3. 点击「保存」。
4. 返回列表，商品名称查询框输入 `TC_PKG_METHOD_071`，点击「查询」，进入编辑页。
5. 进入做法组管理查看 `TC_PKG_METHOD_071` 原始配置。

预期结果：
1. 保存成功，无报错拦截。
2. 套餐商品内展示做法 B 为默认选中、加价为 `2.00`。
3. 做法组管理中原始默认选中与加价不因本次编辑而改变。

---

### 用例编号：TC-需求5-059

用例标题：套餐商品内编辑加料组单次加价和默认选中仅对当前商品生效

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_PKG_ADDON_072`
- extra_0：`TC_PKG_ADDON_EDIT_072`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品
- 就绪条件：页面出现商品列表表格

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_PKG_ADDON_072` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入商品列表页。

测试步骤：
1. 打开 `TC_PKG_ADDON_EDIT_072` 编辑页。
2. 在商品属性中将加料 B 设为默认选中，商品内单次加价改为 `2.00`。
3. 点击「保存」。
4. 返回列表，商品名称查询框输入 `TC_PKG_ADDON_072`，点击「查询」，进入编辑页。
5. 进入加料组管理查看 `TC_PKG_ADDON_072` 原始配置。

预期结果：
1. 保存成功，无报错拦截。
2. 套餐商品内展示加料 B 为默认选中、单次加价为 `2.00`。
3. 加料组管理中原始默认选中与单次加价不因本次编辑而改变。

---

### 用例编号：TC-需求5-060

用例标题：套餐商品内同一选项组仅允许一个默认选中子项

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

数据标识：
- 目标对象：`TC_PKG_ONE_DEFAULT_073`

数据准备：
- 由执行计划/平台造数脚本完成（**不在本文档展开**）；跑本用例前须已按「数据标识」造好对象。

失败分类：
- Readiness 未通过 / 造数未完成 → 环境/数据问题
- 业务断言不符（该失败却成功、提示文案错误等） → 产品缺陷

起始页面：
- 导航：商品管理 → 商品
- 就绪条件：页面出现商品列表表格

前置条件：
1. 已登录商品中心，具备商品管理权限。
2. Readiness：列表按 `TC_PKG_ONE_DEFAULT_073` 可查到目标对象。若查不到 → **环境未就绪（block/skip）**，不判产品缺陷。
3. 从起始页面进入商品列表页。

测试步骤：
1. 商品名称查询框输入 `TC_PKG_ONE_DEFAULT_073`，点击「查询」。
2. 在同一已引用选项组内依次将子项 A、子项 B 设为默认选中。
3. 查看同组选项当前默认选中状态。
4. 点击「保存」。
5. 返回列表，商品名称查询框输入 `TC_PKG_ONE_DEFAULT_073`，点击「查询」，进入编辑页。

预期结果：
1. 同一组选项区域最终仅保留 1 个默认选中子项。
2. 保存后重新进入时，同一组选项内仍仅有 1 个默认选中子项。

---


## S07 补充

### 用例编号：TC-需求5-061

用例标题：套餐组最少选择大于最多选择时保存失败

优先级：P0

执行类型：UI

执行策略：SINGLE_SCENARIO

起始页面：
- 导航：商品管理 → 套餐组
- 就绪条件：页面加载完成

前置条件：
1. 已登录商品中心，具备组管理权限。
2. 从起始页面进入套餐组新增页。

测试步骤：
1. 最少选择填写 `3`。
2. 最多选择填写 `1`。
3. 补全其他必填项并保存。

预期结果：
1. 页面仍停留在当前页，未出现保存/操作成功提示。
2. 提交失败，页面提示最少选择需小于等于最多选择。
3. 未产生不符合预期的数据变更。

---


## S08 补充


## 未匹配自动化源（未写入正文）

| 用例编号 | 用例标题 |
|------|------|
| TC-需求5-062 | 组合搭配单次加价为0元时二次确认 |
| TC-需求5-063 | 套餐商品内编辑组合搭配单次加价仅本商品生效 |
| TC-需求5-064 | 套餐商品内组合搭配默认选中唯一性 |

> 以上条目在商品/组自动化源文件中无同名块，且未配置标题别名；需先在源文件生成后再提取。
